---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/app/watchlist/schemas.py
  - backend/app/watchlist/service.py
  - backend/app/watchlist/router.py
  - backend/tests/watchlist/test_mutation.py
autonomous: true
requirements: [WATCH-02, WATCH-03]

estimate:
  tokens: 34000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - POST /api/watchlist with a valid ticker adds a row to the watchlist, normalizes the ticker to uppercase, and starts streaming that ticker through the market source.
    - POST /api/watchlist with an already-watched or invalid ticker does not create a duplicate row.
    - DELETE /api/watchlist/{ticker} removes the watchlist row, stops the market source from tracking it, and removes it from the price cache.
    - Deleting a ticker that is not watched returns a not-found response without erroring.
  artifacts:
    - backend/app/watchlist/schemas.py
    - backend/app/watchlist/service.py
    - backend/app/watchlist/router.py
  key_links:
    - Watchlist mutation routes call request.app.state.market_source.add_ticker / remove_ticker so the price stream stays in sync with the persisted watchlist.
    - The watchlist table UNIQUE(user_id,ticker) constraint is the backstop against duplicate rows.
---

<objective>
Add watchlist mutation: add a ticker (which also starts streaming it) and remove a ticker (which also stops tracking it), keeping the persisted watchlist, the price cache, and the market data source in sync.

Purpose: Fulfill the mutation half of the watchlist API so the user can grow and prune their watched tickers, with live prices following automatically.
Output: Watchlist request schema, add/remove service functions, POST /api/watchlist and DELETE /api/watchlist/{ticker} endpoints, and their tests.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning/PLAN.md
@backend/CLAUDE.md
@backend/app/main.py
@backend/app/db/database.py
@backend/app/watchlist/service.py
@backend/app/watchlist/router.py
@backend/app/market/interface.py
@backend/app/market/cache.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Add ticker: POST /api/watchlist (WATCH-02)</name>
  <files>backend/app/watchlist/schemas.py, backend/app/watchlist/service.py, backend/app/watchlist/router.py, backend/tests/watchlist/test_mutation.py</files>
  <behavior>
    - Adding PYPL normalizes to uppercase, inserts one watchlist row, and calls market_source.add_ticker so the cache gains a price and the source reports it as tracked.
    - Adding a ticker already in the watchlist does not insert a duplicate row and returns a conflict-or-success response without erroring.
    - Adding an empty or whitespace-only ticker is rejected with a 4xx response and no row is written.
  </behavior>
  <action>
    Create backend/app/watchlist/schemas.py with a pydantic WatchlistAddRequest whose ticker field is a string constrained to non-empty, max length 12, normalized to uppercase stripped. Extend backend/app/watchlist/service.py with add_ticker(conn, market_source, ticker) implementing WATCH-02: insert a new watchlist row (uuid id, user_id 'default', normalized ticker, ISO added_at) using a parameterized insert; when the UNIQUE(user_id,ticker) constraint is already satisfied, return the existing ticker instead of raising; then await market_source.add_ticker(ticker) so the simulator or Massive source begins tracking it. Add POST /api/watchlist to backend/app/watchlist/router.py reading conn and market_source from request.app.state, returning the added ticker (or a conflict status on duplicate), and mapping validation errors to 4xx. Write backend/tests/watchlist/test_mutation.py covering the behaviors above with a temp DB and a mock market source.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/watchlist/test_mutation.py -v</automated>
  </verify>
  <done>POST /api/watchlist adds a normalized ticker, avoids duplicates, and starts streaming it, proven by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Remove ticker: DELETE /api/watchlist/{ticker} (WATCH-03)</name>
  <files>backend/app/watchlist/service.py, backend/app/watchlist/router.py, backend/tests/watchlist/test_mutation.py</files>
  <behavior>
    - Removing an existing ticker deletes its watchlist row, calls market_source.remove_ticker, and leaves the price cache without that ticker.
    - Removing a ticker that is not watched returns a not-found response and leaves the source and cache untouched.
  </behavior>
  <action>
    Extend backend/app/watchlist/service.py with remove_ticker(conn, market_source, ticker) implementing WATCH-03: normalize the ticker, delete the watchlist row where user_id='default' and ticker matches, and when a row was actually deleted await market_source.remove_ticker(ticker) (which also clears it from the price cache); return a boolean indicating whether removal happened. Add DELETE /api/watchlist/{ticker} to backend/app/watchlist/router.py that returns a 204 or not-found status accordingly. Add remove-path tests to backend/tests/watchlist/test_mutation.py.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/watchlist/test_mutation.py -v</automated>
  </verify>
  <done>DELETE /api/watchlist/{ticker} removes the watchlist row and stops streaming that ticker, returning not-found for unknown tickers, proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted JSON body {ticker} crosses into POST /api/watchlist; untrusted path parameter crosses into DELETE /api/watchlist/{ticker} |
| API→SQLite | Watchlist inserts and deletes run against the database |
| API→market source | Ticker strings are forwarded to source.add_ticker / remove_ticker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | POST /api/watchlist and DELETE /api/watchlist/{ticker} input | low | mitigate | Pydantic string with max length 12 and non-empty validation; ticker uppercase-stripped before insert or delete; whitespace-only rejected with 4xx |
| T-03-02 | Tampering | SQL writes in watchlist add/remove | medium | mitigate | Parameterized inserts and deletes only; UNIQUE(user_id,ticker) prevents duplicate rows |
| T-03-03 | Denial of Service | Unbounded watchlist growth via repeated adds | low | accept | Single-user local app; the canonical spec defines no watchlist size cap, and ticker strings are already bounded to max length 12 |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies introduced; pydantic and sqlite3 already present |
</threat_model>

<verification>
cd backend && uv run --extra dev pytest tests/watchlist/ -v
cd backend && uv run --extra dev ruff check app/ tests/
</verification>

<success_criteria>
Adding a ticker persists it and starts streaming, and removing a ticker deletes it and stops streaming, with the watchlist table and price cache kept in sync and invalid or duplicate operations handled safely.
</success_criteria>

<output>
Create .planning/phases/01-backend-foundation/01-03-SUMMARY.md when done
</output>
