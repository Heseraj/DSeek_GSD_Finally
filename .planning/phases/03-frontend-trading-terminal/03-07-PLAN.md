---
phase: 03-frontend-trading-terminal
plan: 07
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - backend/app/main.py
autonomous: false
requirements: [UI-01, UI-02, UI-07]
user_setup:
  - service: OpenRouter
    why: "Optional live (non-mock) chat smoke in the manual browser verification; LLM_MOCK=true covers every automated test without it"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "https://openrouter.ai/keys → create key → add to project-root .env"
    notes: "Optional — skip silently if absent (02-03-SUMMARY.md:193)"
estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "backend/app/main.py gains a DEV-ONLY CORSMiddleware with allow_origins exactly ['http://localhost:3000'] (next dev → FastAPI dev loop), inert in production where builds are same-origin"
    - "The backend still boots and passes ruff after the middleware change"
    - "The executor starts the backend (uvicorn :8000) and the frontend dev server (:3000) itself, and the human confirms the dev-CORS deviation and the running terminal visually"
  artifacts:
    - backend/app/main.py (dev-only CORSMiddleware)
  key_links:
    - "A1 (user-confirmed): the dev-only CORS is the single deviation from the locked no-CORS constraint (PROJECT.md Constraints); production static-export builds keep relative /api and never exercise CORS"
    - "The checkpoint is the trust gate for that deviation — the human must see the actual middleware diff before it ships"
---

<objective>
Dev-loop enabler + manual verification: add the dev-only CORSMiddleware to the backend (assumption A1, user-confirmed) so `next dev` on :3000 can reach FastAPI on :8000, then run the manual browser verification of the complete terminal with a human gate on the CORS deviation.

Purpose: close the phase with the fast dev loop (03-RESEARCH.md:267-272 Pitfall 1) and the manual-only verifications from 03-VALIDATION.md:62-69 (real SSE streaming, chart interaction, visual layout). The CORS change deviates from the locked "no CORS" constraint, so it is gated behind a human checkpoint (per the orchestrator quality gate).
Output: backend serving CORS to localhost:3000 in dev, and a human-signed manual verification of the running terminal.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md
@.planning/phases/03-frontend-trading-terminal/03-PATTERNS.md
@.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <reversibility rating="costly">Dev-only deviation from the locked no-CORS constraint (PROJECT.md Constraints). Undo cost: remove one middleware block; production is unaffected because static-export builds stay same-origin — but the deviation itself needed the human gate, which Task 2 provides.</reversibility>
  <name>Task 1: Add dev-only CORSMiddleware to backend/app/main.py</name>
  <read_first>
    - 03-RESEARCH.md:267-272 (Pitfall 1 + A1), 437-451 (A1 assumption + Open Question 1 — user-confirmed per the phase context)
    - 03-PATTERNS.md:351-365 (main.py pattern: middleware appended after app construction, dev-only comment, production unaffected)
    - backend/app/main.py (current composition — mirror the existing app construction)
  </read_first>
  <files>backend/app/main.py</files>
  <action>
    Per assumption A1 (user-confirmed in the phase planning context): add a DEV-ONLY CORSMiddleware to `backend/app/main.py` so the `next dev` (:3000) → FastAPI (:8000) dev loop works. Implementation:
    - Import `from fastapi.middleware.cors import CORSMiddleware` at the top of main.py.
    - After the existing app construction (routers included — mirror the file's current composition, 03-PATTERNS.md:353-364), call `app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:3000"], allow_methods=["*"], allow_headers=["*"])`.
    - Add a `# DEV ONLY — assumption A1 (user-confirmed): unlocks next dev :3000 -> FastAPI :8000. Inert in production (static-export builds are same-origin; CORS never exercised).` comment above the block.
    - Do NOT add credentials=True (no cookies/credentials are used; the locked constraint says single origin, no CORS in production). Do NOT broaden allow_origins beyond the single dev origin.
    - Run `uv run --extra dev ruff check app/` (clean) and boot the backend to prove it still serves: start `uv run uvicorn app.main:app` in the background (backend workdir), then verify `http://localhost:8000/api/health` returns 200 via `node -e "fetch('http://localhost:8000/api/health').then(r=>{console.log(r.status);process.exit(r.ok?0:1)})"`.
    - Verify the preflight path: an OPTIONS request from origin http://localhost:3000 receives `access-control-allow-origin: http://localhost:3000` (node fetch with method OPTIONS; check the header on the response).
    Commit the main.py change.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev ruff check app/main.py (clean) ; uvicorn boots and /api/health returns 200 ; OPTIONS preflight from http://localhost:3000 returns access-control-allow-origin: http://localhost:3000</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains the CORSMiddleware with allow_origins exactly ['http://localhost:3000'], a DEV-ONLY comment, and no credentials=True
    - `ruff check app/main.py` passes; the backend boots and /api/health returns 200
    - The OPTIONS preflight returns the localhost:3000 origin header (CORS active for the dev origin only)
    - Production path untouched: no other allow_origins, no proxy, no rewrites
  </acceptance_criteria>
  <done>Dev-only CORS is in place, scoped to http://localhost:3000, lint-clean, and the backend boots with the preflight header verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human gate — approve dev-CORS deviation + verify the running terminal in the browser</name>
  <precondition>backend responds at http://localhost:8000/api/health and the frontend dev server responds at http://localhost:3000 — both started by the executor before this checkpoint (uvicorn in backend/, `npm run dev` in frontend/)</precondition>
  <what-built>The complete trading terminal running in dev mode (backend :8000 with the dev-only CORS middleware + frontend `next dev` on :3000), plus the full automated phase battery from 03-06.</what-built>
  <how-to-verify>
    Step 1 — approve the CORS deviation (the gate's purpose): review the backend/app/main.py diff. Confirm: CORSMiddleware allow_origins is exactly ["http://localhost:3000"], a DEV-ONLY comment marks it, no credentials=True, and production static-export builds stay same-origin (relative /api — CORS never exercised). This is the user-confirmed A1 deviation from the locked no-CORS constraint.

    Step 2 — visit http://localhost:3000 and verify the terminal (03-VALIDATION.md:62-69 manual checks):
    1. Dark Bloomberg-style terminal layout: header (live total value, connection dot, cash $10,000.00), watchlist panel with ten tickers, main chart, portfolio section, trade bar, chat panel
    2. Prices flash green/red and sparklines fill in as the SSE stream updates (UI-02)
    3. Connection dot is green (EventSource open); pull the backend down briefly and confirm it turns yellow (reconnecting), then recovers to green
    4. Click a ticker → the larger main chart shows that ticker's live Area chart (UI-03 / success criterion 3)
    5. Buy/sell from the trade bar → cash, positions, and header total update instantly (UI-04)
    6. Portfolio heatmap (treemap), P&L chart, and positions table render with live data (UI-03)
    7. Chat panel: send a message → loading indicator → response with trade/watchlist confirmations inline (UI-05); with LLM_MOCK=true set, no API key is needed; optionally with OPENROUTER_API_KEY set, a live response (skip silently if no key — 02-03-SUMMARY.md:193)
    8. Add a ticker and remove a ticker from the watchlist UI; confirm the removed ticker's sparkline disappears (UI-06)
  </how-to-verify>
  <resume-signal>Type "approved" (or describe the issues to fix, and the executor fixes them and re-runs the affected checks)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev browser (localhost:3000) → backend (:8000) | dev-only cross-origin calls, opened by the CORS middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | dev-only CORSMiddleware | medium | mitigate | allow_origins is exactly ['http://localhost:3000'] — a single dev origin; no wildcard, no credentials=True, DEV-ONLY comment; production static-export builds are same-origin and never exercise CORS (03-RESEARCH.md:536, A1). Gated behind the blocking-human checkpoint because it deviates from the locked no-CORS constraint. |
| T-03-10 | Spoofing | open dev server on :3000 | low | accept | Local dev only, bind default localhost; Phase 4 owns deployment hardening. |
</threat_model>

<verification>
- `cd backend && uv run --extra dev ruff check app/main.py` — lint clean
- `node -e "fetch('http://localhost:8000/api/health').then(r=>{console.log(r.status);process.exit(r.ok?0:1)})"` — backend healthy
- OPTIONS preflight from http://localhost:3000 carries access-control-allow-origin — dev CORS active
- Human checkpoint: CORS diff review + eight manual browser checks signed off
</verification>

<success_criteria>
- Dev loop works: next dev :3000 → FastAPI :8000 with CORS scoped to the dev origin only
- All eight manual browser checks pass and the CORS deviation is human-approved
- Phase 3 is ready for /gsd-verify-work: automated gates green (03-06) + manual verification signed (this plan)
</success_criteria>

<output>
Create `.planning/phases/03-frontend-trading-terminal/03-07-SUMMARY.md` when done
</output>
