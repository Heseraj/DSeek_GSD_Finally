---
phase: 03-frontend-trading-terminal
plan: 06
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04, 03-05]
files_modified:
  - frontend/app/page.tsx
  - frontend/tests/TerminalApp.test.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "page.tsx composes all eight components — Header, WatchlistPanel (full TickerRow rows), MainChart, Heatmap, PnlChart, PositionsTable, TradeBar, ChatPanel — into the dark terminal grid from the architecture diagram"
    - "Every component keeps its per-slice selector subscription; the page body never reads the whole store (20Hz re-render firewall)"
    - "The integration smoke test renders the full terminal under mocked fetch + EventSource: cash, connection dot, watchlist rows, chart, heatmap/positions, trade bar, chat present; an SSE frame updates the header live total; a trade click-through updates the portfolio"
    - "Phase gates green: full vitest suite, npm run build (static export), npx tsc --noEmit"
  artifacts:
    - frontend/app/page.tsx (final composition)
    - frontend/tests/TerminalApp.test.tsx (final integration assertions)
  key_links:
    - "page.tsx is the single composition root — it imports every component from 03-02..03-05 and the store actions from 03-01"
    - "WatchlistPanel row shell (03-05 Task 3) is swapped for the TickerRow composition here — the only cross-plan replacement"
    - "The full-suite + build + tsc gate is the phase's feedback loop before /gsd-verify-work (03-VALIDATION.md:28-32)"
---

<objective>
Integration: compose every component built in 03-02..03-05 into the single-page terminal grid, then run the phase gates — full vitest suite, static export build, and full-project typecheck.

Purpose: deliver the phase goal — "A Bloomberg-style terminal UI with streaming prices, instant trading, portfolio visualizations, and chat, in one page" — as one wired page, and prove the whole system together with an integration smoke test before the manual browser verification in 03-07.
Output: the final `app/page.tsx` terminal and the phase gate results.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md
@.planning/phases/03-frontend-trading-terminal/03-PATTERNS.md
@.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the terminal grid in page.tsx</name>
  <read_first>
    - 03-RESEARCH.md:119-135 (architecture diagram — the exact grid), 244 (whole page 'use client'), 247 (no whole-store reads)
    - 03-PATTERNS.md:90-97 (shell analog: composition root pattern)
  </read_first>
  <files>frontend/app/page.tsx</files>
  <action>
    Rewrite `frontend/app/page.tsx` (already 'use client' from 03-02) into the full composition, replacing the five data-testid slots with the real components:
    - Import and render: `Header` (03-02, top bar spanning the full width), `WatchlistPanel` (03-05) in the left column — replace its self-contained row shell with the full `TickerRow` composition: pass the store watchlist into WatchlistPanel which renders `<TickerRow ticker={t.ticker} />` per entry (03-05 Task 3 deferred this swap here by design), `MainChart` (03-03) in the center column above the portfolio section, the portfolio section = `Heatmap` + `PnlChart` + `PositionsTable` (03-04) below the chart, `TradeBar` (03-05) under the portfolio, and `ChatPanel` (03-05) in the right column.
    - Grid: dark terminal layout per the architecture diagram — Tailwind grid/flex with `h-screen`, `min-w-0`, panel backgrounds from the 03-01 tokens (`#1a1a2e` panels, `#30363d` borders, `#0d1117` page background); the chat panel column keeps a fixed comfortable width with its own scroll area; the center column scrolls vertically.
    - Keep `usePriceStream()` mounted once and the mount-time `refetchPortfolio()` + `refetchWatchlist()` effect from 03-02.
    - SELECTOR DISCIPLINE (Pitfall 6): the page body may read only `useStore(s => s.watchlist)` for the row list (and that only inside WatchlistPanel); no other whole-store or broad-slice reads at page level. Every price/history/connection read stays in the leaf components built earlier.
    - Verify the build + typecheck per the plan verification below before committing.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` emits out/index.html with all components imported (no unused-slot leftovers)
    - `npx tsc --noEmit` clean
    - page.tsx imports all eight components; no data-testid slot stubs remain except where a component genuinely owns the slot
    - No whole-store reads in page.tsx (only per-slice or component-level selectors)
  </acceptance_criteria>
  <done>The full terminal grid composes and builds as a static export.</done>
</task>

<task type="auto">
  <name>Task 2: Integration smoke test + phase gates</name>
  <read_first>
    - 03-VALIDATION.md:28-32 (sampling rate / phase gate: full suite + build + tsc), 41 (TerminalApp map row)
    - 03-RESEARCH.md:494 (UI-01 TerminalApp behavior), 29 (header live total)
  </read_first>
  <files>frontend/tests/TerminalApp.test.tsx</files>
  <action>
    Extend `frontend/tests/TerminalApp.test.tsx` (from 03-02) with the integration assertions:
    1. With mocked fetch (portfolio with 2 positions + cash 10000, watchlist with 2 tickers, history snapshots) and a captured EventSource instance, render the full `page.tsx`: assert the header shows $10,000.00 cash and the green connection dot after an `open` event; assert watchlist rows render for both tickers; assert the main chart container, heatmap, P&L chart, positions table, trade bar, and chat panel are all present in the DOM.
    2. Dispatch an SSE frame for one ticker → the header live total re-renders to the recomputed value (cash + Σ qty × live price).
    3. Interaction smoke: click a watchlist row → store.selectedTicker is set (main chart re-seeds); type a quantity into the trade bar and click Buy → mocked fetch receives the trade POST and the portfolio section reflects the returned cash.
    4. Keep the 03-02 XSS-guard assertion (no parsed elements from a tag-shaped string in any text render path).

    Then run the PHASE GATES and record the results in the summary:
    - `cd frontend && npx vitest run` — full suite green
    - `cd frontend && npm run build` — static export compiles
    - `cd frontend && npx tsc --noEmit` — full-project typecheck clean
    Commit the test extension, then the gates result.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TerminalApp.test.tsx passes with the four integration assertions
    - Full vitest suite green, npm run build succeeds, npx tsc --noEmit clean — all three recorded in the SUMMARY
    - No plan-level MISSING verification entries remain for UI-01..UI-07 (every requirement now has an automated test path)
  </acceptance_criteria>
  <done>The integrated terminal is proven end-to-end by tests, and the phase gates are green before manual verification.</done>
</task>

</tasks>

## Artifacts this phase produces

- `frontend/app/page.tsx` — the final composition root: full terminal grid wiring `Header` + `WatchlistPanel` (with the `TickerRow` swap) + `MainChart` + `Heatmap` + `PnlChart` + `PositionsTable` + `TradeBar` + `ChatPanel`, `usePriceStream` mounted once, mount-time refetch effect
- `frontend/tests/TerminalApp.test.tsx` — final integration smoke (all panels, SSE frame → header live total, ticker click → chart re-seed, trade click-through, XSS guard at composed-page level)
- Phase gate results recorded in the SUMMARY: full vitest suite, `npm run build`, `npx tsc --noEmit`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed components → single page | all previously isolated trust decisions converge in one DOM tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | full-page rendering | high | mitigate | The integration test re-asserts the XSS guard at the composed-page level (no parsed elements from tag-shaped strings in any text render path) — carries 03-05's mitigation into the final tree (ASVS V5). |
| T-03-09 | DoS | 20Hz re-render at composition | medium | mitigate | Composition keeps per-slice subscriptions: page.tsx holds no whole-store reads; integration test runs under jsdom with mocked EventSource so the full tree renders without jank (Pitfall 6, 03-RESEARCH.md:295-299). |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full frontend suite
- `cd frontend && npm run build` — static export compiles
- `cd frontend && npx tsc --noEmit` — full-project typecheck
</verification>

<success_criteria>
- The single page renders the complete terminal: header (live total + connection + cash), watchlist with sparklines, main chart, heatmap, P&L chart, positions table, trade bar, chat panel
- Integration smoke proves live updates propagate across panels from one SSE frame
- All phase gates green; every UI-XX requirement has an automated test path
</success_criteria>

<output>
Create `.planning/phases/03-frontend-trading-terminal/03-06-SUMMARY.md` when done
</output>
