---
phase: 03-frontend-trading-terminal
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/components/portfolio/Heatmap.tsx
  - frontend/components/portfolio/PnlChart.tsx
  - frontend/components/portfolio/PositionsTable.tsx
  - frontend/tests/Heatmap.test.tsx
  - frontend/tests/PnlChart.test.tsx
  - frontend/tests/PositionsTable.test.tsx
autonomous: true
requirements: [UI-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Heatmap renders a Recharts Treemap sized by market_value and colored by unrealized_pnl sign/intensity (green=profit, red=loss, gray=zero)"
    - "PnlChart renders a Recharts LineChart over GET /api/portfolio/history snapshots and re-polls every 30s to match the server snapshot cadence"
    - "PositionsTable renders ticker/quantity/avg_cost/current_price/market_value/unrealized_pnl/unrealized_pnl_percent with the current_price column reading live SSE prices (fallback to position.current_price)"
    - "All three components render an empty state ('No positions' / 'No history yet') when their data is absent, and never crash on null portfolio"
  artifacts:
    - frontend/components/portfolio/Heatmap.tsx
    - frontend/components/portfolio/PnlChart.tsx
    - frontend/components/portfolio/PositionsTable.tsx
    - frontend/tests/Heatmap.test.tsx
    - frontend/tests/PnlChart.test.tsx
    - frontend/tests/PositionsTable.test.tsx
  key_links:
    - "Heatmap/PositionsTable read store.portfolio (03-01) — the trade/chat plans refetch it; live price column reads store.prices per row"
    - "PnlChart polls /api/portfolio/history via apiFetch (03-01) with a 30s interval matching backend snapshots.py cadence (A5)"
    - "Components are leafs: no page.tsx dependency — 03-06 wires them into the shell"
---

<objective>
Portfolio visualizations: the heatmap treemap (sized by market value, colored by P&L), the P&L line chart over portfolio history snapshots, and the positions table with a live price column. UI-03's three components, built in parallel with the tracer (03-02) and controls (03-05).

Purpose: deliver success criterion 4 ("The portfolio heatmap (treemap), P&L line chart, and positions table render with live data") using Recharts 3.10.1 — the locked choice for low-frequency declarative SVG. These are leaf components proven by their own component tests; the shell wiring lands in 03-06.
Output: three tested components ready to drop into the terminal grid.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md
@.planning/phases/03-frontend-trading-terminal/03-PATTERNS.md
@.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md
@backend/app/portfolio/schemas.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heatmap treemap — sized by market_value, colored by P&L</name>
  <read_first>
    - 03-RESEARCH.md:392-400 (Recharts Treemap verbatim — dataKey="size" nameKey="name", custom content), 313-319 (Position fields)
    - 03-PATTERNS.md:263-273 (Heatmap pattern + pnl color scale)
  </read_first>
  <behavior>
    - Test 1: with a store portfolio containing positions [{ticker:'AAPL', market_value: 5000, unrealized_pnl: 250}, {ticker:'TSLA', market_value: 3000, unrealized_pnl: -150}], the Treemap receives data [{name:'AAPL', size:5000, pnl:250}, {name:'TSLA', size:3000, pnl:-150}]
    - Test 2: a positive-pnl cell carries a green fill class, a negative-pnl cell a red fill, zero-pnl a gray fill
    - Test 3: with an empty portfolio the component renders 'No positions' and no Treemap
  </behavior>
  <files>frontend/components/portfolio/Heatmap.tsx, frontend/tests/Heatmap.test.tsx</files>
  <action>
    Write `frontend/tests/Heatmap.test.tsx` FIRST (RED): seed the store portfolio via useStore.setState, render Heatmap, assert the three behaviors (Recharts renders to SVG in jsdom — query the data by text/labels and by passing a spy `content` render function where needed). Confirm RED, then implement.

    Create `frontend/components/portfolio/Heatmap.tsx` (GREEN): reads `useStore(s => s.portfolio)`; empty state text 'No positions' when portfolio is null or positions is empty; otherwise renders `<Treemap data={positions.map(p => ({ name: p.ticker, size: p.market_value, pnl: p.unrealized_pnl }))} dataKey="size" nameKey="name" stroke="#1a1a2e" content={HeatmapCell} />` per 03-RESEARCH.md:395-399 inside a ResponsiveContainer with an explicit height (h-48). `HeatmapCell` is a custom content renderer: rect fill = pnl > 0 ? emerald-scaled-by-|pnl|/maxAbsPnl : pnl < 0 ? red-scaled : gray (compute maxAbsPnl = max(|pnl|) over positions; alpha-scaled rgba fills — green rgba(16,185,129,alpha), red rgba(239,68,68,alpha), zero #30363d); label = ticker + fmtCurrency(market_value). Import Treemap, ResponsiveContainer from 'recharts'.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/Heatmap.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    - Heatmap.test.tsx passes: data mapping, color-by-pnl, empty state
    - Heatmap.tsx uses Recharts Treemap with dataKey/nameKey per 03-RESEARCH.md:395-399 and never renders when portfolio is null
  </acceptance_criteria>
  <done>Portfolio heatmap treemap renders positions sized by market value and colored by P&L.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P&L line chart over /api/portfolio/history with 30s polling</name>
  <read_first>
    - 03-RESEARCH.md:321 (HistoryResponse shape), 441 (A5: poll every ~30s to match snapshots.py cadence)
    - 03-PATTERNS.md:275-277 (PnlChart pattern: LineChart dataKey="total_value", XAxis dataKey="recorded_at", dark grid)
  </read_first>
  <behavior>
    - Test 1: on mount, PnlChart fetches GET /api/portfolio/history via apiFetch and renders one line point per snapshot {recorded_at, total_value}
    - Test 2: after 30s of fake timers the component fetches again (poll interval matches the server snapshot cadence)
    - Test 3: while no data is loaded it shows a loading/empty state; with an empty snapshots array it shows 'No history yet'
  </behavior>
  <files>frontend/components/portfolio/PnlChart.tsx, frontend/tests/PnlChart.test.tsx</files>
  <action>
    Write `frontend/tests/PnlChart.test.tsx` FIRST (RED): mock fetch to return `{snapshots: [{recorded_at: '2026-08-26T10:00:00Z', total_value: 10000}, ...]}`; use vi.useFakeTimers to assert the 30s re-poll; assert line data rendering and the empty/loading states. Confirm RED, then implement.

    Create `frontend/components/portfolio/PnlChart.tsx` (GREEN): a useEffect that calls `apiFetch<HistoryResponse>('/api/portfolio/history')` immediately and sets `setInterval` for a 30,000ms re-poll (cleanup clears the interval — A5); holds snapshots in local state; renders Recharts `LineChart` inside ResponsiveContainer h-48: `Line dataKey="total_value"` (stroke #209dd7, dot false), `XAxis dataKey="recorded_at"` with a tickFormatter that renders HH:MM, `YAxis` domain ['auto', 'auto'], dark CartesianGrid stroke #1a1a2e; loading state 'Loading…' while snapshots is null; 'No history yet' when the array is empty.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/PnlChart.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    - PnlChart.test.tsx passes: initial fetch + render, 30s re-poll under fake timers, loading/empty states
    - PnlChart.tsx polls /api/portfolio/history at 30s cadence and clears the interval on unmount
  </acceptance_criteria>
  <done>P&L line chart renders portfolio value over time and stays current with the server snapshot cadence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Positions table with live SSE price column</name>
  <read_first>
    - 03-RESEARCH.md:313-319 (Position/PortfolioResponse fields verbatim), 25 (live current-price column from SSE)
    - 03-PATTERNS.md:279-281 (per-row live price selector with fallback to position.current_price)
  </read_first>
  <behavior>
    - Test 1: with two positions in the store, the table renders rows with ticker, quantity, avg_cost, current_price, market_value, unrealized_pnl, unrealized_pnl_percent
    - Test 2: after applyPrices sets a newer SSE price for one ticker, that row's current_price column shows the live price instead of position.current_price (fallback works for the other row)
    - Test 3: pnl cells carry emerald/red classes via pnlColor; empty portfolio renders 'No positions'
  </behavior>
  <files>frontend/components/portfolio/PositionsTable.tsx, frontend/tests/PositionsTable.test.tsx</files>
  <action>
    Write `frontend/tests/PositionsTable.test.tsx` FIRST (RED): seed portfolio + prices in the store, render, assert the three behaviors. Confirm RED, then implement.

    Create `frontend/components/portfolio/PositionsTable.tsx` (GREEN): reads `useStore(s => s.portfolio)`; empty state 'No positions'; otherwise a table with columns ticker | quantity | avg_cost | current_price | market_value | unrealized_pnl | unrealized_pnl_percent; the current_price cell reads `useStore(s => s.prices[row.ticker]?.price)` per row with `?? row.current_price` fallback (03-PATTERNS.md:281); pnl + pnl% cells use pnlColor (03-01 format.ts) + fmtCurrency/fmtPercent; market_value is recomputed-live-agnostic (render the server's market_value — the authoritative value; the price column alone reflects the live stream). Row key = ticker.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/PositionsTable.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    - PositionsTable.test.tsx passes: all seven columns, live price with fallback, pnl colors, empty state
    - Each row subscribes only to its own price slice (no whole-store reads)
  </acceptance_criteria>
  <done>Positions table renders with a live SSE-driven current-price column.</done>
</task>

</tasks>

## Artifacts this phase produces

- `frontend/components/portfolio/Heatmap.tsx` — component `Heatmap` + internal `HeatmapCell` custom Treemap content renderer (size = market_value, fill = pnl sign/intensity)
- `frontend/components/portfolio/PnlChart.tsx` — component `PnlChart` (Recharts LineChart over `/api/portfolio/history`, 30s poll, loading/empty states)
- `frontend/components/portfolio/PositionsTable.tsx` — component `PositionsTable` (7 columns, live SSE price column with fallback, pnlColor rows, empty state)
- Test files: `frontend/tests/Heatmap.test.tsx`, `frontend/tests/PnlChart.test.tsx`, `frontend/tests/PositionsTable.test.tsx`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portfolio REST data → SVG/DOM | server responses render as treemap cells, chart points, table cells (numbers only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | portfolio/positions display | low | accept | All displayed values originate from backend responses or type-guarded SSE frames; forged client state alters only the cosmetic rendering, and any mutation re-validates server-side (03-RESEARCH.md:537). |
| T-03-02 | Tampering | live price column | low | accept | The column reads store.prices (03-01 type-guarded); a malformed frame cannot inject markup — text rendering only. |
</threat_model>

<verification>
- `cd frontend && npx vitest run tests/Heatmap.test.tsx tests/PnlChart.test.tsx tests/PositionsTable.test.tsx -q` — portfolio suite
- `cd frontend && npx tsc --noEmit` — typecheck clean
</verification>

<success_criteria>
- Heatmap, P&L chart, and positions table each render from mocked backend data and update from live store prices
- The three components are standalone leafs (no page.tsx edits) ready for 03-06 wiring
</success_criteria>

<output>
Create `.planning/phases/03-frontend-trading-terminal/03-04-SUMMARY.md` when done
</output>
