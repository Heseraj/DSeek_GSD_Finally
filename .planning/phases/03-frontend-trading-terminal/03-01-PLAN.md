---
phase: 03-frontend-trading-terminal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/.env.local
  - frontend/app/globals.css
  - frontend/postcss.config.mjs
  - frontend/vitest.config.ts
  - frontend/tests/setup.ts
  - frontend/lib/types.ts
  - frontend/lib/api.ts
  - frontend/lib/format.ts
  - frontend/store/useStore.ts
  - frontend/tests/useStore.test.ts
autonomous: true
requirements: [UI-01, UI-02, UI-07]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "frontend/ scaffolds with Next.js 16 + TS + Tailwind 4 and builds to frontend/out/index.html under output:'export' (no Node server produced)"
    - "TypeScript contracts in lib/types.ts match the backend Pydantic shapes exactly (PriceUpdate, Position, PortfolioResponse, HistoryResponse, WatchlistResponse, TradeActionResult, WatchlistChangeResult, ChatResponse)"
    - "apiUrl()/apiFetch() resolve NEXT_PUBLIC_API_BASE ('' in prod builds → same-origin /api/*)"
    - "zustand store applies {TICKER: PriceUpdate} frames with 100-point-capped histories and per-ticker tickSeq; connection state and portfolio/watchlist/chat slices exist"
    - "vitest runs in jsdom with jest-dom matchers, a capture-instance EventSource mock, a global fetch mock, and a lightweight-charts module mock"
  artifacts:
    - frontend/package.json
    - frontend/next.config.ts
    - frontend/.env.local
    - frontend/app/globals.css
    - frontend/vitest.config.ts
    - frontend/tests/setup.ts
    - frontend/lib/types.ts
    - frontend/lib/api.ts
    - frontend/lib/format.ts
    - frontend/store/useStore.ts
    - frontend/tests/useStore.test.ts
  key_links:
    - "store actions consumed by every later plan (applyPrices, setConnection, selectTicker, pruneTicker, refetchPortfolio, refetchWatchlist, chat actions) — signatures must match 03-RESEARCH.md:202-215"
    - "tests/setup.ts mocks are the contract every later test file relies on (EventSource dispatch, fetch stub, lightweight-charts stub)"
---

<objective>
Greenfield foundation for the Phase 3 frontend: scaffold the Next.js 16 static-export SPA, install the locked dependency set, transcribe the backend API contracts into TypeScript, build the zustand store, and stand up the Vitest harness. This is the Wave-0 layer every later plan imports from.

Purpose: everything in 03-02..03-07 compiles against the types, api helpers, store slices, and test mocks created here. Getting the contracts verbatim and the store semantics right now is what prevents rework across seven plans.
Output: a `frontend/` directory that builds to `out/`, with `lib/`, `store/`, `vitest.config.ts`, `tests/setup.ts` in place and a green store unit test.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md
@.planning/phases/03-frontend-trading-terminal/03-PATTERNS.md
@.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md
@backend/app/portfolio/schemas.py
@backend/app/chat/schemas.py
@backend/app/watchlist/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend/ (create-next-app), lock deps, static-export config, dark theme tokens</name>
  <read_first>
    - 03-RESEARCH.md:45-87 (Standard Stack + Installation), 189-194 (project structure), 229-233 (Pattern 4 API base)
    - 03-PATTERNS.md:54-87 (scaffold patterns: command, next.config.ts, .env.local)
  </read_first>
  <files>frontend/ (scaffold output), frontend/package.json, frontend/next.config.ts, frontend/.env.local, frontend/app/globals.css, frontend/postcss.config.mjs</files>
  <action>
    Run from the repo root: `npx --yes create-next-app@latest frontend --typescript --app --eslint --tailwind --use-npm --turbopack`, accepting any remaining prompts with defaults (import alias `@/*`, no src dir). This scaffolds Next 16.3.3 + React 19.2.8 + Tailwind 4.3.3 (per 03-RESEARCH.md:17, A6).

    Then, inside `frontend/`:
    1. `npm install lightweight-charts@5.2.1 recharts@3.10.1 zustand@5.0.15` (the only three runtime deps beyond the scaffold — 03-RESEARCH.md:80-85).
    2. `npm install -D vitest@4.1.11 @testing-library/react@16.3.2 @testing-library/jest-dom@7.0.1 jsdom@30.0.1`.
    3. Edit `frontend/next.config.ts` to `const nextConfig: NextConfig = { output: 'export' };` — the single mandatory deviation from the scaffold default (03-PATTERNS.md:66-70). Do NOT add rewrites/proxy: they are unsupported with `output:'export'` (03-RESEARCH.md:78). This makes `npm run build` emit `frontend/out/` with one HTML file per route and no Node server.
    4. Create `frontend/.env.local` containing exactly `NEXT_PUBLIC_API_BASE=http://localhost:8000` (dev only — 03-PATTERNS.md:82-86). Confirm `git check-ignore frontend/.env.local` passes (scaffold .gitignore covers `.env*`; if not, append `.env.local` to `frontend/.gitignore`). Never put any other value or secret in NEXT_PUBLIC_* (T-03-05: only the API base URL may be inlined; prod builds leave it unset → `''` → same-origin relative /api).
    5. Confirm `frontend/postcss.config.mjs` uses the Tailwind v4 `@tailwindcss/postcss` plugin (scaffold default; if a legacy v3 config appeared, switch it per tailwindcss.com/docs/installation/framework-guides/nextjs).
    6. Edit `frontend/app/globals.css`: keep `@import "tailwindcss";` and add the dark-theme `@theme` tokens (`#0d1117` background, `#1a1a2e` panel, `#30363d` border — PROJECT.md Constraints) plus the flash keyframes/classes verbatim from 03-RESEARCH.md:388-389 and 03-PATTERNS.md:98-107: `@keyframes flash-up` (rgba(38,166,154,.35) → transparent), `@keyframes flash-down` (rgba(197,48,48,.35) → transparent), `.flash-up { animation: flash-up .5s ease-out; }`, `.flash-down { animation: flash-down .5s ease-out; }`.
  </action>
  <verify>
    <automated>cd frontend && npm run build (exits 0) && Test-Path out/index.html; npm ls lightweight-charts recharts zustand (reports 5.2.1 / 3.10.1 / 5.0.15)</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in frontend/ exits 0 and `frontend/out/index.html` exists (static export produced)
    - `npm ls lightweight-charts recharts zustand` reports exactly 5.2.1, 3.10.1, 5.0.15
    - `frontend/next.config.ts` contains `output: 'export'` and no rewrites/proxy
    - `frontend/.env.local` contains only NEXT_PUBLIC_API_BASE and is git-ignored
    - globals.css contains the dark @theme tokens and both flash keyframes + classes
  </acceptance_criteria>
  <done>Scaffold + pinned deps + static-export config committed; build emits out/index.html; dark theme tokens and flash animation CSS in place.</done>
</task>

<task type="auto">
  <name>Task 2: Backend contract types, apiUrl/apiFetch helpers, formatters, vitest harness</name>
  <read_first>
    - 03-RESEARCH.md:303-336 (verbatim TypeScript contract types — copy from here, not from memory), 402-416 (apiUrl/apiFetch code example)
    - 03-PATTERNS.md:111-158 (types/api/format patterns incl. the 204-empty-body note at 143), 428-434 (test harness pattern)
    - 03-VALIDATION.md:16-24 (vitest config contract)
  </read_first>
  <files>frontend/lib/types.ts, frontend/lib/api.ts, frontend/lib/format.ts, frontend/vitest.config.ts, frontend/tests/setup.ts</files>
  <action>
    Create `frontend/lib/types.ts` as a verbatim transcription of 03-RESEARCH.md:303-336 (which quotes the backend Pydantic schemas): `PriceUpdate` (direction: 'up'|'down'|'flat'), `Position`, `PortfolioResponse`, `HistoryResponse` ({snapshots: {recorded_at: string; total_value: number}[]}), `WatchlistResponse` (union `PriceUpdate | { ticker: string }`), `TradeActionResult`, `WatchlistChangeResult`, `ChatResponse` with `error?: string | null` OPTIONAL so the 503-with-valid-body contract type-checks without special-casing (03-PATTERNS.md:120-122). No `any`/`unknown`; the SSE frame is typed `Record<string, PriceUpdate>`.

    Create `frontend/lib/api.ts` per 03-RESEARCH.md:402-416: `const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? '';` then `export const apiUrl = (p: string) => `${API_BASE}${p}`;` and `export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>` that sets `Content-Type: application/json`, throws `Error(\`${path} -> ${res.status}\`)` on `!res.ok`, and returns `res.json()`. The DELETE 204 empty-body case and the chat 503 body-on-error case are handled by callers, NOT here (03-PATTERNS.md:141-143); do not special-case them in apiFetch.

    Create `frontend/lib/format.ts` per 03-PATTERNS.md:151-157: `fmtCurrency(n)` and `fmtPercent(n)` via `Intl.NumberFormat` (never `toFixed` sprinkling — 03-RESEARCH.md:260), and `pnlColor(n: number): string` returning 'text-emerald-400' | 'text-red-400' | 'text-gray-400'.

    Create `frontend/vitest.config.ts` per 03-RESEARCH.md:486: `environment: 'jsdom'`, `setupFiles: ['./tests/setup.ts']`.

    Create `frontend/tests/setup.ts` per 03-RESEARCH.md:511 + 03-PATTERNS.md:428-434:
    1. `import '@testing-library/jest-dom/vitest'` (registers toBeInTheDocument/toHaveClass matchers).
    2. EventSource class mock: a class assigned to `globalThis.EventSource` whose constructor captures the instance into a shared registry; exposes `__emit(type, event)` helpers so tests dispatch synthetic `open` / `error` / `message` events (`message` carries `{ data: JSON.stringify(frame) }`); `close()` records a closed flag; `readyState` is mutable for the CONNECTING/OPEN/CLOSED mapping tests.
    3. Global fetch mock: `vi.stubGlobal('fetch', vi.fn())` so every test file can `vi.mocked(fetch).mockResolvedValue(...)` without per-file stubbing.
    4. lightweight-charts module mock via `vi.mock('lightweight-charts', ...)` with a `vi.hoisted` factory: `createChart` returns `{ addSeries: () => ({ setData: vi.fn(), update: vi.fn() }), timeScale: () => ({ fitContent: vi.fn() }), applyOptions: vi.fn(), remove: vi.fn() }` (chart instances also captured into the hoisted registry for lifecycle assertions); `AreaSeries`, `LineSeries`, `ColorType` exported as plain objects/constants. This lets canvas components render safely in jsdom (real canvas measurement is impossible there; 03-VALIDATION.md:66-67 covers real rendering manually).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in frontend/ exits 0 (all contract types + helpers type-check)
    - lib/types.ts interfaces match 03-RESEARCH.md:303-336 field-for-field; ChatResponse.error is optional
    - lib/api.ts throws `${path} -> ${status}` on non-ok and inlines NEXT_PUBLIC_API_BASE
    - vitest.config.ts runs jsdom with setupFiles ./tests/setup.ts
    - tests/setup.ts registers jest-dom, the capture-instance EventSource mock, a global fetch stub, and the lightweight-charts hoisted mock
  </acceptance_criteria>
  <done>Contracts, api helpers, formatters, and the test harness exist and compile; later plans import them without modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: zustand store — SSE merge, connection state, portfolio/watchlist/chat actions</name>
  <read_first>
    - 03-RESEARCH.md:196-198 (Pattern 1 selector isolation), 202-215 (store shape verbatim), 290-293 (Pitfall 5: prune on watchlist removal)
    - 03-PATTERNS.md:161-187 (store patterns: functional set, 100-point cap A4, tickSeq, derived live total)
  </read_first>
  <behavior>
    - Test 1: applyPrices({AAPL: frame}) merges the frame into prices and appends the price to histories.AAPL; a second frame for the same ticker appends again
    - Test 2: histories[ticker] is capped at 100 points (101st update drops the oldest)
    - Test 3: tickSeq[ticker] increments only when a frame's direction differs from the current price's direction (flash key-remount source)
    - Test 4: setConnection('connected'|'reconnecting'|'closed') sets connection
    - Test 5: selectTicker sets selectedTicker
    - Test 6: pruneTicker(t) removes prices[t], histories[t], and tickSeq[t] (Pitfall 5)
    - Test 7: refetchPortfolio() GETs /api/portfolio via apiFetch and stores PortfolioResponse
    - Test 8: refetchWatchlist() GETs /api/watchlist and maps the union into WatchlistTicker[] ({ticker} vs PriceUpdate)
    - Test 9: appendChatMessage / setChatLoading update the chat slices
  </behavior>
  <files>frontend/store/useStore.ts, frontend/tests/useStore.test.ts</files>
  <action>
    Write `frontend/tests/useStore.test.ts` FIRST (RED): create the store by rendering a harness-free import of `useStore` (zustand vanilla `useStore.getState()`/`useStore.setState()` work without React), mock `fetch` from setup.ts for the refetch tests, and encode the nine behaviors above with exact assertions (history cap at 100, tickSeq only on direction change, prune removes all three slices). Run `npx vitest run tests/useStore.test.ts -q` and confirm it fails with a missing-module error (RED).

    Then create `frontend/store/useStore.ts` per the store shape at 03-RESEARCH.md:202-215 (GREEN):
    - State: `prices: Record<string, PriceUpdate>`, `histories: Record<string, number[]>`, `tickSeq: Record<string, number>`, `connection: 'connected'|'reconnecting'|'closed'`, `selectedTicker: string|null`, `portfolio: PortfolioResponse|null`, `watchlist: WatchlistTicker[]` (WatchlistTicker = { ticker: string; price?: number }), `chatMessages: { role: 'user'|'assistant'; content: string; trades?: TradeActionResult[]; watchlist_changes?: WatchlistChangeResult[]; error?: string|null }[]`, `chatLoading: boolean`.
    - `applyPrices(data: Record<string, PriceUpdate>)`: functional `set(state => ...)` that merges each entry; appends `price` to `histories[ticker]` capped at 100 (A4); increments `tickSeq[ticker]` when `data[ticker].direction` differs from the current `prices[ticker]?.direction`; defensively skips entries whose shape fails a type guard (missing ticker/price/direction or direction not in {'up','down','flat'}) so a malformed frame never corrupts state (T-03-02 — the store is the second line of defense after the hook's try/catch).
    - `setConnection`, `selectTicker`, `pruneTicker` (removes prices/histories/tickSeq — Pitfall 5), `refetchPortfolio` (`apiFetch<PortfolioResponse>('/api/portfolio')` → set), `refetchWatchlist` (`apiFetch<WatchlistResponse>('/api/watchlist')` → map union to WatchlistTicker[]), `appendChatMessage`, `setChatLoading`.
    - Derived selector `selectLiveTotal(state)`: `portfolio ? cash_balance + Σ(quantity × (prices[ticker]?.price ?? current_price)) : 0` (03-RESEARCH.md:29, 03-PATTERNS.md:185).
    Commit RED then GREEN per the phase TDD convention.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/useStore.test.ts -q (all pass)</automated>
  </verify>
  <acceptance_criteria>
    - All nine behavior tests pass in tests/useStore.test.ts
    - histories capped at 100; tickSeq increments only on direction change; pruneTicker removes prices + histories + tickSeq
    - refetchPortfolio/refetchWatchlist issue GETs through apiFetch and write the mapped results
    - selectLiveTotal computes cash + Σ(qty × live price) with SSE-price fallback to current_price
  </acceptance_criteria>
  <done>Store unit suite green; every store action later plans depend on exists with the RESEARCH-documented semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend/ | untrusted third-party packages enter the project at install time |
| backend SSE/REST → store | parsed frames and responses cross into client state (defense designed here) |
| build-time env → bundle | NEXT_PUBLIC_* values get inlined into shipped JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (next, react, lightweight-charts, recharts, zustand, vitest, @testing-library/react, jsdom) | high | mitigate | Package Legitimacy Audit completed in 03-RESEARCH.md:89-112 — all packages approved (six SUS rows are release-date heuristic artifacts with first-party repos, no postinstall scripts); no checkpoint warranted per 03-RESEARCH.md:110. Install exact pinned versions from the audit table. |
| T-03-05 | Information Disclosure | NEXT_PUBLIC_* env in bundle | medium | mitigate | Only NEXT_PUBLIC_API_BASE is inlined, and it is a URL not a credential; .env.local is git-ignored; the action forbids any other NEXT_PUBLIC_* value (03-RESEARCH.md:538). |
| T-03-02 | Tampering | store.applyPrices | medium | mitigate | applyPrices type-guards every frame entry (direction ∈ {up,down,flat}, numeric price) and skips malformed entries — the store never evaluates data (03-RESEARCH.md:245, 534-535); the hook's try/catch is the first line (03-02). |
</threat_model>

<verification>
- `cd frontend && npm run build` — static export still compiles after each task
- `cd frontend && npx tsc --noEmit` — full-project typecheck clean
- `cd frontend && npx vitest run tests/useStore.test.ts -q` — store unit suite green
</verification>

<success_criteria>
- frontend/ exists, builds to out/index.html with output:'export', and holds exactly the three locked runtime deps at pinned versions
- lib/types.ts is a verbatim backend-contract transcription; lib/api.ts and lib/format.ts compile; vitest + setup.ts run jsdom tests with EventSource/fetch/lightweight-charts mocks
- useStore.ts provides every action later plans call, with applyPrices cap/tickSeq/prune semantics proven by tests
</success_criteria>

<output>
Create `.planning/phases/03-frontend-trading-terminal/03-01-SUMMARY.md` when done
</output>
