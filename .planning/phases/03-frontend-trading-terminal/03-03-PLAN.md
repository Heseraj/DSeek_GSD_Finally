---
phase: 03-frontend-trading-terminal
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - frontend/components/chart/useLightweightChart.ts
  - frontend/components/chart/MainChart.tsx
  - frontend/components/watchlist/Sparkline.tsx
  - frontend/components/watchlist/TickerRow.tsx
  - frontend/tests/MainChart.test.tsx
  - frontend/tests/TickerRow.test.tsx
autonomous: true
requirements: [UI-01, UI-03]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "useLightweightChart encapsulates the official v5 lifecycle: createChart in useEffect, resize listener → applyOptions({width}), chart.remove() in cleanup"
    - "MainChart renders an Area series for the selected ticker from store histories; per-tick updates go through series.update({time: Math.floor(ts), value}) and setData is used only when the selected ticker changes"
    - "Every watchlist row renders a Sparkline fed only its history array, streaming via series.update"
    - "Chart containers carry explicit heights (h-96 main, h-8 sparkline) so createChart never measures a 0-height container (Pitfall 2)"
    - "Canvas lifecycle is verified under jsdom via the setup.ts lightweight-charts mock (createChart/update/remove call assertions), real rendering deferred to the manual browser check (03-VALIDATION.md:66)"
  artifacts:
    - frontend/components/chart/useLightweightChart.ts
    - frontend/components/chart/MainChart.tsx
    - frontend/components/watchlist/Sparkline.tsx
    - frontend/tests/MainChart.test.tsx
  key_links:
    - "MainChart/Sparkline subscribe to store histories + selectedTicker (03-01 slices) — per-slice selectors only"
    - "TickerRow (03-02) gains the Sparkline import — this is the only cross-plan edit, which is why this plan sits in Wave 3 after 03-02"
    - "Math.floor(timestamp) at the series boundary — backend sends float Unix seconds, lightweight-charts intraday expects integer seconds (Pitfall 3)"
---

<objective>
Charting expansion from the proven tracer slice: the shared lightweight-charts v5 lifecycle hook, the selected-ticker main Area chart, and the per-row sparklines that fill in as SSE prices accumulate.

Purpose: deliver UI-02's sparkline visual and the main chart for UI-03/success criterion 3 ("Clicking a ticker shows a larger chart") using the locked two-library charting split — lightweight-charts 5.2.1 for streaming canvas (main + sparklines), Recharts later for portfolio visuals (03-04). No candlestick is possible: the backend streams only `price`, no OHLC — the main chart is Area/Line (03-RESEARCH.md:431).
Output: clicking any watchlist ticker drives a large Area chart on the selected ticker's live history; every row shows a live sparkline.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md
@.planning/phases/03-frontend-trading-terminal/03-PATTERNS.md
@.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useLightweightChart hook + MainChart (selected ticker, streaming Area series)</name>
  <read_first>
    - 03-RESEARCH.md:357-377 (official TradingView v5 React wrapper — the exact createChart/addSeries(AreaSeries)/remove lifecycle), 279-282 (Pitfall 3: Math.floor for float timestamps), 273-277 (Pitfall 2: explicit container height), 248 (never setData per tick)
    - 03-PATTERNS.md:238-261 (chart pattern notes: v5 API, addSeries(AreaSeries) not addAreaSeries, resize listener)
  </read_first>
  <behavior>
    - Test 1: MainChart calls createChart exactly once on mount with a dark-theme layout (background #0d1117) and an explicit container width from the ref
    - Test 2: after the store's histories[AAPL] gains points, series.update is called with {time: integer (Math.floor of the timestamp), value} — never setData after the initial seed
    - Test 3: when the selected ticker changes, the series is re-seeded with setData for the new ticker's history and subsequent updates use update()
    - Test 4: unmount calls chart.remove() (and removes the resize listener)
  </behavior>
  <files>frontend/components/chart/useLightweightChart.ts, frontend/components/chart/MainChart.tsx, frontend/tests/MainChart.test.tsx</files>
  <action>
    Write `frontend/tests/MainChart.test.tsx` FIRST (RED): use the setup.ts lightweight-charts mock registry (chart instances + series spies captured via the hoisted mock) to assert the four behaviors; seed the store with `useStore.setState({ selectedTicker: 'AAPL', histories: { AAPL: [...] } })`; append history via applyPrices to drive update calls; confirm RED (missing modules).

    Then create (GREEN):
    1. `frontend/components/chart/useLightweightChart.ts` — a hook wrapping the official v5 lifecycle (03-RESEARCH.md:360-376): takes a ref + options; in useEffect calls `createChart(ref.current, { layout: { background: { type: ColorType.Solid, color: '#0d1117' }, textColor: '#c9d1d9' }, width: ref.current.clientWidth, height })`; registers `window.addEventListener('resize', onResize)` where onResize calls `chart.applyOptions({ width: ref.current.clientWidth })`; returns `{ chart, series }` where series is `chart.addSeries(AreaSeries, { lineColor: '#209dd7', topColor: 'rgba(32,157,215,0.4)', bottomColor: 'rgba(32,157,215,0.02)' })`; cleanup removes the listener and calls `chart.remove()`. Import `AreaSeries, createChart, ColorType` from 'lightweight-charts' (v5 API — never the v4 addAreaSeries method).
    2. `frontend/components/chart/MainChart.tsx` — subscribes `useStore(s => s.selectedTicker)` and `useStore(s => s.histories[s.selectedTicker])` (per-slice; the history array is the only per-ticker data the component receives). Container div with explicit `h-96` (Pitfall 2). Chart behavior: on selectedTicker change, re-seed the series via `series.setData(points)` where points = histories.map((v, i) => ({ time: i, value: v })) — index-based time is stable for a full re-seed; on history growth while the ticker is unchanged, `series.update({ time: Math.floor(lastTimestamp), value: lastValue })` — the timestamps come from the store's PriceUpdate frames, always Math.floor'ed at this boundary (Pitfall 3); if the new point's floored time equals the last bar's time, update() replaces the last bar (correct v5 behavior). Track the last time per ticker in a ref so re-seeds and updates never regress. `chart.timeScale().fitContent()` after seeding.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/MainChart.test.tsx -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - MainChart.test.tsx: all four behaviors pass (single createChart, update-with-integer-time, setData only on ticker switch, remove on unmount)
    - useLightweightChart.ts exists as the single chart-lifecycle owner; MainChart consumes it
    - No setData call in the per-tick path; no float timestamps reach the series
  </acceptance_criteria>
  <done>Main chart renders the selected ticker's live Area series with streaming updates and clean lifecycle, proven by mock-based lifecycle tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sparkline component + wire into TickerRow</name>
  <read_first>
    - 03-RESEARCH.md:357-377 (v5 wrapper), 298 (sparkline receives only its history array — Pitfall 6)
    - 03-PATTERNS.md:236-237 (TickerRow sparkline slot), 261 (explicit height h-8)
  </read_first>
  <behavior>
    - Test 1: TickerRow renders a Sparkline container with data-testid `sparkline-{ticker}`
    - Test 2: the Sparkline receives the store history array for its ticker only (not the whole histories map)
    - Test 3: appending a history point triggers series.update on the sparkline's chart; the chart is removed on unmount
  </behavior>
  <files>frontend/components/watchlist/Sparkline.tsx, frontend/components/watchlist/TickerRow.tsx, frontend/tests/TickerRow.test.tsx</files>
  <action>
    Extend `frontend/tests/TickerRow.test.tsx` FIRST with the three sparkline behaviors (RED): assert the data-testid slot, that the history prop equals `store.histories[ticker]` only, and via the setup.ts mock registry that update fires on history growth. Confirm RED, then implement.

    Create `frontend/components/watchlist/Sparkline.tsx` (GREEN): props `{ ticker: string; data: number[] }`; uses the useLightweightChart hook from 03-03 Task 1 with an explicit `h-8` container; seeds `series.setData(data.map((v, i) => ({ time: i, value: v })))` on mount/data reset and `series.update({ time: data.length - 1, value: last })` per appended point (index-based time — the store history is a plain number[]; no float timestamps reach this series). Modify `frontend/components/watchlist/TickerRow.tsx` to replace the empty sparkline slot div (03-02 Task 1) with `<Sparkline ticker={ticker} data={useStore(s => s.histories[ticker])} />` — the component receives only its array (Pitfall 6). Run the extended TickerRow suite to GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/TickerRow.test.tsx -q && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TickerRow.test.tsx passes including the three sparkline behaviors
    - Sparkline.tsx uses useLightweightChart with an h-8 container and index-based time points
    - TickerRow passes `useStore(s => s.histories[ticker])` only; no whole-store subscription
    - `npm run build` still compiles (TickerRow now imports Sparkline)
  </acceptance_criteria>
  <done>Every watchlist row renders a live sparkline that fills in as SSE prices accumulate, with per-array selector isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store histories → canvas series | numeric history values cross into the charting library (data-only, no HTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | DoS | series.update with non-monotonic/float time | medium | mitigate | Math.floor at the series boundary + last-time-per-ticker ref; update() with a regressed time throws in v5 — the guard prevents the throw (Pitfall 3, 03-RESEARCH.md:279-282). |
| T-03-02 | Tampering | history arrays feeding charts | low | accept | Histories are populated only from type-guarded applyPrices (03-01); charts render numbers, never markup (03-RESEARCH.md:534-535). |
</threat_model>

<verification>
- `cd frontend && npx vitest run tests/MainChart.test.tsx tests/TickerRow.test.tsx -q` — charting suite
- `cd frontend && npm run build` — static export compiles with canvas components
- `cd frontend && npx tsc --noEmit` — typecheck clean
</verification>

<success_criteria>
- Clicking a ticker shows a larger Area chart of its live history; sparklines fill in on every watchlist row
- Chart lifecycle (create/update/remove) is owned by one hook and proven by mock-based lifecycle tests
- Real canvas rendering is deferred to the manual browser verification in 03-07 (03-VALIDATION.md:66)
</success_criteria>

<output>
Create `.planning/phases/03-frontend-trading-terminal/03-03-SUMMARY.md` when done
</output>
