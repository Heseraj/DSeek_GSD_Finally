---
context: phase
phase: 03-frontend-trading-terminal
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-08-27T04:22:15.794Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Do not re-dispatch an executor subagent for 03-07 Task 2.** The prior session's executor was interrupted TWICE at this same blocking-human gate — it halts at the checkpoint and cannot self-advance. The correct resume action is to present the CORS diff + 8 manual browser checks to the user directly in the orchestrator, collect the verdict, then finish (SUMMARY → phase.complete).

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-dispatching a subagent into a blocking-human gate | The 03-07 executor returns `## CHECKPOINT REACHED` and stops; twice the session ended there. The gate cannot be auto-passed. | blocking | On resume, present the gate inline to the user yourself; only write SUMMARY + mark phase complete after the human verdict. |
| Executor interrupted mid-commit | The first 03-07 continuation agent wrote the CORS code but was interrupted before committing; the second was interrupted after committing Task 1 but before SUMMARY. | advisory | Always verify git state + SUMMARY existence before assuming a plan finished; re-anchor on the actual on-disk state. |

## Critical Anti-Patterns (do NOT repeat these)
- **Re-dispatching subagents into the 03-07 blocking-human gate** → present the gate to the user inline; finish the phase in the orchestrator.

<current_state>
FinAlly Phase 3 (Frontend Trading Terminal) is 6/7 plans complete. Plan 03-07 Task 1 (dev-only CORSMiddleware, assumption A1, user-approved) is DONE and committed as `f4b745e`. The ONLY remaining work is **03-07 Task 2**: a blocking-human gate — the user must approve the CORS diff and verify the running terminal in a real browser (8 manual checks). Both dev servers are currently running (uvicorn :8000, next dev :3000).
</current_state>

<completed_work>

Completed Tasks (all committed):
- 03-01 Foundation: create-next-app (Next 16.3.3, output:'export', Tailwind 4), contract types, apiUrl/apiFetch, zustand store, vitest harness — 12 tests
- 03-02 Terminal tracer: usePriceStream SSE hook, flashing TickerRow, Header connection dot + live total — TDD
- 03-04 Portfolio visuals: heatmap treemap, P&L line chart (30s poll), positions table with live prices — TDD
- 03-05 Controls: TradeBar instant fill, ChatPanel (503-with-ChatResponse contract, text-only), WatchlistPanel (409/204/404) — TDD
- 03-03 Charting: useLightweightChart v5 hook, MainChart, Sparkline embedded in TickerRow — TDD
- 03-06 Integration: full 8-component terminal grid in page.tsx, TickerRow remove wiring (raw DELETE), integration smoke test — 58 tests total
- 03-07 T1: dev-only CORSMiddleware added to backend/app/main.py, committed `f4b745e`

Phase gates at 03-06: 58 vitest tests, npm run build (out/index.html), npx tsc --noEmit, npm run lint — all green. Backend: 158 pytest tests (Phase 2), ruff clean.
</completed_work>

<remaining_work>

- **03-07 Task 2 (the ONLY remaining work in Phase 3):** blocking-human gate — approve CORS deviation + 8-step manual browser verification. Steps (03-07-PLAN.md:97-105):
  1. Dark Bloomberg-style terminal layout: header (live total, connection dot, cash $10,000.00), watchlist with ten tickers, main chart, portfolio section, trade bar, chat panel
  2. Prices flash green/red and sparklines fill in as SSE delivers updates (UI-02)
  3. Connection dot green; pull backend down briefly → yellow (reconnecting) → recovers to green
  4. Click a ticker → larger main chart shows that ticker's live Area chart (UI-03)
  5. Buy/sell from trade bar → cash, positions, header total update instantly (UI-04)
  6. Heatmap, P&L chart, positions table render with live data (UI-03)
  7. Chat: send a message → loading → response with confirmations inline (UI-05); LLM_MOCK=true covers no-key; live optional if OPENROUTER_API_KEY set
  8. Add + remove a watchlist ticker; removed ticker's sparkline disappears (UI-06)
- After approval: write 03-07-SUMMARY.md → `phase.complete "3"` → Phase 4 (Deployment + E2E: DEPLOY-01..04, TEST-01..02; Docker single container, persistent SQLite, Playwright; watch FastAPI app.frontend() gap — installed 0.128.7 lacks it, RESEARCH A2/03-RESEARCH.md:451-454)
</remaining_work>

<decisions_made>

- Dev-only CORSMiddleware (allow_origins=['http://localhost:3000'], no credentials, DEV-ONLY comment) — A1 user-confirmed; production static builds stay same-origin (03-07-PLAN.md:95)
- Stack: Next 16.3.3 output:'export' + lightweight-charts 5.2.1 + recharts 3.10.1 + zustand 5.0.15 + vitest 4.1.11 (03-RESEARCH.md, all registry-verified)
- No candlestick — backend streams price only (no OHLC); main chart is Area (lightweight-charts v5 addSeries(AreaSeries))
- UI-06 remove: raw fetch DELETE (apiFetch res.json() rejects on 204) + pruneTicker + refetchWatchlist, wired in TickerRow
- Phase 3 skipped discuss-phase + CONTEXT.md (canonical spec §10-13 exhaustive); research + planning directly
</decisions_made>

<blockers>
- None. One human action pending: the 03-07 Task 2 browser verification + CORS approval.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-frontend-trading-terminal/03-07-PLAN.md` — Task 2's 8 manual checks + resume-signal
2. `.planning/phases/03-frontend-trading-terminal/03-VALIDATION.md:62-69` — manual-only verification list
3. `.planning/ROADMAP.md` — Phase 3/4 details, success criteria
4. `.planning/STATE.md` — project memory, current position
5. `backend/app/main.py` — the committed CORS middleware diff (`git show f4b745e`)
6. `.planning/phases/03-frontend-trading-terminal/03-RESEARCH.md` — Phase 4 notes (FastAPI app.frontend() gap, deployment)

## Infrastructure State
- uvicorn on :8000 (PID 35500) — backend with dev CORS middleware
- next dev on :3000 (PID 20728) — frontend, NEXT_PUBLIC_API_BASE=http://localhost:8000 (frontend/.env.local)
- Backend deps: uv-managed (Python 3.12); frontend deps: npm (Node 24.19.0)
- LLM_MOCK=true recommended for chat testing (no OPENROUTER_API_KEY on machine)
- Working tree clean except untracked tooling artifacts (.gsd/, node_modules/, backend/db/, .opencode/*/lib/, milestone.lock — leave them)

## Pre-Execution Critique Required
- Not applicable (pausing at a human-verify gate, not between design and execution).

<context>
Phase 3 built the complete frontend trading terminal and it is fully automated-test-passing (58 tests). The phase's final gate is a human sign-off on the running app in a real browser — the only thing an automated agent cannot do. The two interruptions both occurred at this exact gate. The cleanest resume: load http://localhost:3000 in a browser, walk the 8 checks, then approve. After that the phase seals trivially.
</context>

<next_action>
Start with: present the CORS diff (`git show f4b745e`) + the 8 manual browser checks to the user, ask for their verdict on http://localhost:3000. On approval, write 03-07-SUMMARY.md, run `phase.complete "3"`, commit, then plan Phase 4 (Deployment + E2E).
</next_action>
