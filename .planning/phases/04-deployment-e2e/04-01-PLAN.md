---
phase: 04-deployment-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/uv.lock
  - backend/tests/test_app.py
  - .gitignore
  - db/.gitkeep
  - Dockerfile
  - .dockerignore
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "backend/app/main.py reads FINALLY_DB_PATH (default 'db/finally.db') and serves the static export via app.frontend('/') with check_dir=False, so backend-only pytest still imports app.main without frontend/out present"
    - "backend/uv.lock pins fastapi 0.141.1 (uv add fastapi@0.141.1) and the full backend pytest suite stays green after the bump"
    - "docker build -t finally:latest . succeeds with a 3-stage image (node:22-slim build → uv python3.12-slim-trixie deps → runtime); the runtime stage runs as non-root USER app and contains no node/uv source"
    - "docker run -v finally-data:/app/db -p 8000:8000 finally:latest serves the terminal at /, returns /api/health {'status': 'healthy'}, streams SSE frames over /api/stream/prices, and a REST buy of 5 AAPL survives docker restart (cash_balance < 10000 after restart)"
  artifacts:
    - backend/app/main.py
    - backend/uv.lock
    - backend/tests/test_app.py
    - .gitignore
    - db/.gitkeep
    - Dockerfile
    - .dockerignore
  key_links:
    - "Dockerfile WORKDIR /app + COPY --from=frontend-build /build/out /app/static ↔ app.frontend(directory='static') ↔ FINALLY_DB_PATH default 'db/finally.db' → /app/db/finally.db (exact DEPLOY-03 volume target — Pitfall 2)"
    - ".dockerignore exclusion of frontend/.env.local ↔ NEXT_PUBLIC_API_BASE stays unset in the build → relative /api/* same-origin (Pitfall 3, T-04-05)"
    - "uv sync --locked in the image build ↔ the Task-1-bumped uv.lock (fastapi 0.141.1) — build must not run before the bump"
---

<objective>
The phase tracer: one production end-to-end path through every layer this phase touches — backend contract edits (FINALLY_DB_PATH env read + app.frontend static mount + FastAPI 0.141.1 bump), the 3-stage Dockerfile, the named-volume runtime, and the HTTP surface the container must serve. When Task 3's chain is green, the phase architecture is proven: a single Docker command builds and serves the working terminal on :8000 with a persistent database.

Purpose: this is the architectural dead-end detector for the whole phase. Proving "one container serves static + API + SSE + persistent SQLite" here catches layout mistakes (WORKDIR, static dir name, DB path, check_dir) after one commit instead of after the scripts and E2E stack are layered on top.
Output: backend/app/main.py + backend/uv.lock + backend/tests/test_app.py modified, .gitignore + db/.gitkeep + Dockerfile + .dockerignore created, and the `finally:latest` image built, booted, and proven end-to-end on :8000.

Note on tracer ordering: the quality gate requires the FINALLY_DB_PATH + fastapi bump + app.frontend changes to be sequenced BEFORE the Docker build tasks, so the tracer (Task 3) sits behind its two wave-0 prerequisites inside this plan — the hard sequencing constraint overrides the usual "tracer first" task order.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-e2e/04-RESEARCH.md
@.planning/phases/04-deployment-e2e/04-PATTERNS.md
@.planning/phases/04-deployment-e2e/04-VALIDATION.md
@.planning/phases/03-frontend-trading-terminal/03-07-SUMMARY.md
@backend/app/main.py
@backend/tests/test_app.py
@backend/pyproject.toml
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend contract — FINALLY_DB_PATH env read, app.frontend mount, FastAPI 0.141.1 bump, env-read test, gitignore/db placeholder</name>
  <files>backend/app/main.py, backend/uv.lock, backend/tests/test_app.py, .gitignore, db/.gitkeep</files>
  <read_first>
    - 04-RESEARCH.md:13 (FINALLY_DB_PATH recommendation), 165-173 (Pattern 1 app.frontend), 218-222 (Pitfall 1 check_dir), 287-297 (main.py code changes), 461-470 (Wave 0 gaps)
    - 04-PATTERNS.md:42-73 (main.py edits incl. the import-os gotcha), 76-84 (uv bump), 88-107 (new test pattern), 111-127 (.gitignore)
    - backend/app/main.py (imports lines 5-13, DB_PATH line 33, include_router block line 79, CORS block lines 81-88)
    - backend/tests/test_app.py:17-20 (_make_client fixture), 23-33 (TestAppSmoke class conventions)
  </files>backend/app/main.py, backend/uv.lock, backend/tests/test_app.py, .gitignore, db/.gitkeep</files>
  <behavior>
    - Test 1 (env read): with FINALLY_DB_PATH set to a tmp path and app.main reloaded, main.DB_PATH equals that tmp path — red on today's hardcoded string, green after the env read
    - Test 2 (regression): the existing TestAppSmoke suite (health, seeding, price cache, SSE) stays green — the module-attribute monkeypatch overrides still win over the env read
  </behavior>
  <action>
    1. Run `cd backend && uv add fastapi@0.141.1` (pyproject.toml already allows fastapi>=0.115.0 — no manifest edit; uv regenerates backend/uv.lock pinning fastapi 0.141.1). Then `uv sync --extra dev` and confirm `uv run python -c "import fastapi; print(fastapi.__version__)"` prints 0.141.1. litellm 1.98.0 declares no fastapi constraint (04-RESEARCH.md:49, verified at backend/uv.lock:910-937) — the bump touches nothing else. This is a reversible change (uv can downgrade).

    2. Edit backend/app/main.py with exactly three edits (04-PATTERNS.md:42-73):
       a. Add `import os` to the import block (lines 5-13 import only asyncio/contextlib/logging/dotenv/fastapi/CORSMiddleware — the PATTERNS gotcha: the env read is impossible without this third edit, 04-PATTERNS.md:57).
       b. Replace line 33 `DB_PATH: str = "db/finally.db"` with `DB_PATH: str = os.environ.get("FINALLY_DB_PATH", "db/finally.db")` — locked decision A4; default preserves today's behavior for any dev run without the env var; existing tests override the module attribute after import so all 10+ monkeypatch call sites stay green (04-RESEARCH.md:13, 297).
       c. After `app.include_router(chat_router)` (line 79), append `app.frontend("/", directory="static", check_dir=False)` — placed BEFORE the DEV-ONLY CORS middleware block (lines 81-88). `check_dir=False` (never the default "auto") is mandatory: frontend/out/ is gitignored, so backend-only pytest must import app.main without a build present — the default would raise at app creation (Pitfall 1, 04-RESEARCH.md:218-222).

    3. Add the env-read unit test to backend/tests/test_app.py inside class TestAppSmoke, verbatim pattern from 04-PATTERNS.md:97-107: method `test_db_path_reads_finally_db_path_env(self, tmp_path, monkeypatch)` — monkeypatch.setenv("FINALLY_DB_PATH", str(tmp_path / "env.db")); then import importlib, import app.main as main, set main.DB_PATH = None, importlib.reload(main), and assert main.DB_PATH == str(tmp_path / "env.db"). Follow the file's conventions (module docstring, class grouping, pytest built-ins only — no new fixtures). The reload re-executes app.main top-level (recreating the module-level `app`); sibling tests hold their own `app` binding from module import time, so test ordering within the class is irrelevant and no other test is affected.

    4. Edit root .gitignore: add `db/finally.db` and `db/finally.db-journal` directly after the existing `db.sqlite3` / `db.sqlite3-journal` lines (lines 60-62) — today only db.sqlite3 is ignored, so a runtime db/finally.db would be tracked by git (04-RESEARCH.md:467).

    5. Create db/.gitkeep as an empty file so the db/ directory survives git while the runtime DB file is ignored. The runtime named volume mounts over /app/db inside the container — this placeholder is host-repo-only (04-PATTERNS.md:195-198).
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest -q</automated>
    <automated>cd backend && uv run --extra dev ruff check app/main.py tests/test_app.py</automated>
    <automated>cd backend && uv run python -c "import fastapi; print(fastapi.__version__)" # prints 0.141.1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import fastapi; print(fastapi.__version__)"` in backend/ prints 0.141.1 and backend/uv.lock shows fastapi version 0.141.1
    - backend/app/main.py contains `import os` in the import block, the DB_PATH env read on line 33, and the app.frontend call with check_dir=False after the include_router block
    - `uv run --extra dev pytest -q` in backend/ passes the full suite including the new test_db_path_reads_finally_db_path_env
    - `uv run --extra dev ruff check app/main.py tests/test_app.py` exits 0
    - root .gitignore contains db/finally.db and db/finally.db-journal; db/.gitkeep exists
  </acceptance_criteria>
  <done>Backend contract edits committed: env-configurable DB_PATH, static export mounted via app.frontend with check_dir=False, fastapi 0.141.1 pinned in uv.lock, env-read unit test green, db/finally.db gitignored with db/.gitkeep in place.</done>
</task>

<task type="auto">
  <name>Task 2: 3-stage Dockerfile (node:22 build → uv deps → non-root runtime) + .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - 04-RESEARCH.md:255-285 (Dockerfile skeleton — the authoritative pattern source), 137-139 (.dockerignore required set), 196-201 (anti-patterns: no --workers>1, no StaticFiles catch-all), 230-234 (Pitfall 3 .env.local), 387 (A1 node:22 user-confirmed), 391 (A5 non-root)
    - 04-PATTERNS.md:130-193 (Dockerfile + .dockerignore patterns, non-negotiables list)
  </files>Dockerfile, .dockerignore</files>
  <action>
    Create the root Dockerfile with three stages, following 04-RESEARCH.md:255-284 verbatim (uv Docker guide + FastAPI container guide; do NOT copy the stale cached finally:latest image's layout — WORKDIR /app/backend + inert FINALLY_DB_PATH — 04-RESEARCH.md:9, 381):

    - Stage frontend-build: FROM node:22-slim (locked decision A1, USER-CONFIRMED — deviation from REQUIREMENTS' "Node 20" because Node 20 hit EOL 2026-04 and Next 16 requires >=20.9; 04-RESEARCH.md:51, 387). WORKDIR /build. COPY frontend/package.json frontend/package-lock.json ./ then RUN npm ci (deps layer cached; npm 10 on node:22 runs lifecycle scripts normally — no allowScripts interference). COPY frontend/ . then RUN npm run build (output:'export' emits out/).
    - Stage backend-deps: FROM ghcr.io/astral-sh/uv:python3.12-slim-trixie. WORKDIR /app. COPY backend/pyproject.toml backend/uv.lock ./ then RUN --mount=type=cache,target=/root/.cache/uv uv sync --locked --no-install-project --no-editable (deps layer, cacheable — --locked REQUIRES the Task 1-bumped uv.lock, so this task must not run before Task 1). COPY backend/ . then RUN --mount=type=cache,target=/root/.cache/uv uv sync --locked --no-editable (bakes app into site-packages).
    - Stage runtime: FROM ghcr.io/astral-sh/uv:python3.12-slim-trixie. WORKDIR /app (CRITICAL: the relative default db/finally.db resolves to /app/db/finally.db — the exact DEPLOY-03 volume target; Pitfall 2, 04-RESEARCH.md:224-228). COPY --from=backend-deps /app/.venv /app/.venv. COPY --from=frontend-build /build/out /app/static (the directory name MUST match app.frontend(directory="static")). ENV PATH="/app/.venv/bin:$PATH". RUN useradd --create-home --uid 1000 app && mkdir -p /app/db && chown -R app:app /app/db then USER app (non-root hardening, A5 — mitigates T-04-02; the documented A5 fallback is reverting to root if Docker Desktop volume-permission friction appears, 04-RESEARCH.md:391). EXPOSE 8000. CMD ["/app/.venv/bin/uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"] — exec form (required for graceful shutdown/lifespan); use the venv binary directly, NOT uv run (no project file in the runtime stage); NEVER --workers > 1 — PriceCache, simulator, and snapshot loop are process-local state (04-RESEARCH.md:37, 196).

    Create the root .dockerignore with exactly the 04-RESEARCH.md:137-139 set: .git, **/node_modules, frontend/.next, frontend/out, frontend/.env.local, backend/.venv, **/__pycache__, .pytest_cache, .ruff_cache, .planning/, .env. frontend/.env.local is the critical entry — it holds NEXT_PUBLIC_API_BASE=http://localhost:8000; if it leaks into the build context, Next inlines absolute URLs into the export and every port remap / non-localhost host breaks all API calls (Pitfall 3, T-04-05). frontend/out is excluded so the host's gitignored build never leaks into the image (A8). Never use ENV in the Dockerfile for secrets — keys arrive only via --env-file at docker run (T-04-01).
  </action>
  <verify>
    <automated>docker build -t finally:latest .</automated>
  </verify>
  <acceptance_criteria>
    - `docker build -t finally:latest .` from the repo root exits 0 (first build ~2-5 min; cache mounts reused on rebuild)
    - The Dockerfile has exactly three FROM stages: node:22-slim, ghcr.io/astral-sh/uv:python3.12-slim-trixie (x2 — deps and runtime), with exec-form uvicorn CMD and no --workers flag
    - The runtime stage has USER app, chown'd /app/db, WORKDIR /app, and COPY --from=frontend-build /build/out /app/static
    - .dockerignore contains all of: .git, **/node_modules, frontend/.next, frontend/out, frontend/.env.local, backend/.venv, .planning/, .env
  </acceptance_criteria>
  <done>finally:latest builds successfully from the 3-stage Dockerfile; .dockerignore keeps .env.local and build artifacts out of the context.</done>
</task>

<task type="tracer">
  <name>TRACER: build → run → serve → SSE → trade → restart persistence → non-root identity (one path end-to-end)</name>
  <files>Dockerfile, .dockerignore, backend/app/main.py</files>
  <read_first>
    - 04-RESEARCH.md:99-131 (system architecture diagram — the exact runtime contract), 248-251 (Pitfall 6 health poll), 287-297 (main.py health endpoint), 224-228 (Pitfall 2 volume check)
    - 04-PATTERNS.md:443-451 (health-poll gate pattern, /api/health returns {"status": "healthy"})
    - .planning/phases/03-frontend-trading-terminal/03-07-SUMMARY.md (static export lands in frontend/out/; backend serves it)
  </files>Dockerfile, .dockerignore, backend/app/main.py</files>
  <action>
    The phase tracer: one production path from docker build to a working, persistent, single-container deployment. Run from the repo root with Docker Desktop running (04-RESEARCH.md:413 — check `docker info` first, start Docker Desktop at C:\Program Files\Docker\Docker\Docker Desktop.exe if the daemon is down). PowerShell 7 host syntax:

    1. Ensure the Task 2 image is current: `docker build -t finally:latest .`.
    2. Remove any stale container: `docker rm -f finally 2>$null | Out-Null`, then `docker run -d --name finally -v finally-data:/app/db -p 8000:8000 finally:latest`. The named volume finally-data is auto-created and mounted at /app/db (DEPLOY-03).
    3. Health-poll gate (Pitfall 6): loop up to 30 x 2s on http://localhost:8000/api/health until it returns 200 — uvicorn takes several seconds to import litellm; do not assert before the poll.
    4. Assert the HTTP surface: GET / returns the terminal HTML (index.html contains __next markers), GET /api/health returns {"status":"healthy"}, and GET /api/stream/prices emits SSE frames (curl.exe -s -N --max-time 4 piped through Select-String 'data:' — the stream is infinite so the timeout is mandatory).
    5. DEPLOY-03 restart persistence: POST /api/portfolio/trade with body {"ticker":"AAPL","side":"buy","quantity":5} (seed tickers include AAPL, primed at startup), then `docker restart finally`, health-poll again, then GET /api/portfolio — cash_balance must be below 10000 (the trade survived the restart; the volume held the DB).
    6. Runtime identity: `docker exec finally whoami` must print app (non-root, T-04-02) and `docker exec finally ls /app/db` must show finally.db (volume mount exact — Pitfall 2).
    7. Leave the container running: wave 2's script idempotency tests (04-02) start with `docker rm -f finally` so a live container is expected and handled.
  </action>
  <verify>
    <automated>docker rm -f finally 2>$null | Out-Null; docker run -d --name finally -v finally-data:/app/db -p 8000:8000 finally:latest; $h=$false; foreach($i in 1..30){ try { if((Invoke-WebRequest -UseBasicParsing http://localhost:8000/api/health -TimeoutSec 3).StatusCode -eq 200){$h=$true;break} } catch {}; Start-Sleep -Seconds 2 }; if(-not $h){throw 'health poll failed'}; $html=(Invoke-WebRequest -UseBasicParsing http://localhost:8000/ -TimeoutSec 10).Content; if($html -notmatch '__next'){throw 'static export not served'}; curl.exe -s -N --max-time 4 http://localhost:8000/api/stream/prices | Select-String -Pattern 'data:' | Select-Object -First 1; Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/portfolio/trade -ContentType 'application/json' -Body '{"ticker":"AAPL","side":"buy","quantity":5}' | Out-Null; docker restart finally; $h2=$false; foreach($i in 1..30){ try { if((Invoke-WebRequest -UseBasicParsing http://localhost:8000/api/health -TimeoutSec 3).StatusCode -eq 200){$h2=$true;break} } catch {}; Start-Sleep -Seconds 2 }; if(-not $h2){throw 'restart health poll failed'}; $p=Invoke-RestMethod -Uri http://localhost:8000/api/portfolio -TimeoutSec 10; if($p.cash_balance -ge 10000){throw 'trade did not survive restart'}; docker exec finally whoami; docker exec finally ls /app/db</automated>
  </verify>
  <acceptance_criteria>
    - `docker run -d --name finally -v finally-data:/app/db -p 8000:8000 finally:latest` starts and /api/health returns 200 within 60 seconds
    - GET / returns HTML matching `__next` (the static Next.js export is served at root)
    - /api/stream/prices emits at least one `data:` SSE frame within 4 seconds
    - After POST /api/portfolio/trade (buy 5 AAPL) and `docker restart finally`, GET /api/portfolio reports cash_balance strictly below 10000
    - `docker exec finally whoami` prints `app`; `docker exec finally ls /app/db` lists finally.db
  </acceptance_criteria>
  <done>The single-container path is proven end-to-end and committed: one image builds, boots, serves the terminal + API + SSE on :8000, and persists a REST trade across a container restart; the runtime is non-root and the volume lands exactly at /app/db/finally.db.</done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 04-01)

- `backend/app/main.py` — modified: `import os`, `DB_PATH = os.environ.get("FINALLY_DB_PATH", "db/finally.db")`, `app.frontend("/", directory="static", check_dir=False)` registered after the routers
- `backend/uv.lock` — regenerated: fastapi pinned at 0.141.1 (litellm untouched)
- `backend/tests/test_app.py` — new `TestAppSmoke.test_db_path_reads_finally_db_path_env`
- `.gitignore` — new entries `db/finally.db`, `db/finally.db-journal`
- `db/.gitkeep` — new empty placeholder
- `Dockerfile` — 3 stages: `frontend-build` (node:22-slim), `backend-deps` (uv python3.12-slim-trixie), runtime (non-root USER app, WORKDIR /app, exec-form uvicorn CMD)
- `.dockerignore` — 11 entries incl. frontend/.env.local, frontend/out, frontend/.next, **/node_modules, .env
- Image: `finally:latest`; container name `finally`; volume `finally-data` mounted at `/app/db`
- Env var contract: `FINALLY_DB_PATH` (default `db/finally.db`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → container (docker run) | build context + runtime env injection crosses here; secrets arrive only via --env-file |
| build context → image layers | Dockerfile ENV/COPY decides what is baked into the image permanently |
| container process → SQLite volume | runtime writes the DB; volume ownership must match the runtime user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Dockerfile ENV / image layers | high | mitigate | Keys only via `--env-file`/`-e` at `docker run` (never ENV in the Dockerfile); `.dockerignore` excludes `.env` and `frontend/.env.local`; scripts pass `--env-file .env` only when the file exists (ASVS V8) |
| T-04-02 | Elevation of Privilege | Container runtime | medium | mitigate | Non-root `USER app` (uid 1000) + `mkdir -p /app/db && chown -R app:app /app/db` in the Dockerfile (A5); verified in the tracer via `docker exec finally whoami` (ASVS V14) |
| T-04-03 | Tampering | Base images / dependency supply chain | high | mitigate | Digest-tag-pinned base images (node:22-slim, ghcr.io/astral-sh/uv:python3.12-slim-trixie — never latest); `npm ci` from committed lockfiles; `uv sync --locked` from committed uv.lock; fastapi pinned exactly 0.141.1 with litellm verified free of a fastapi constraint (ASVS V14) |
| T-04-04 | Tampering | npm postinstall / script execution | medium | mitigate | `npm ci` from committed package-lock.json only; frontend allowScripts scoped to `next: true`; test runner package `@playwright/test` has `postinstall: null` (audit, 04-RESEARCH.md:82-93) |
| T-04-05 | Tampering | NEXT_PUBLIC_API_BASE inlining (broken same-origin) | medium | mitigate | `.dockerignore` excludes `frontend/.env.local` + `**/.env*` so the production build inlines `''` → relative `/api/*`; same-origin design means CORS is never exercised (Pitfall 3) |
| T-04-06 | Spoofing / Tampering | E2E container vs production data | high | mitigate | Dedicated `finally-test-data` volume in the test compose (never `finally-data`); run-e2e leads with `down -v` (04-03/04-04) |
| T-04-SC | Tampering | npm/pip installs | low | mitigate | Package Legitimacy Audit present (04-RESEARCH.md:82-93): `@playwright/test`/`playwright` approved first-party (Microsoft), `postinstall: null`, exact-pinned; `[SUS]` verdicts are the documented release-date heuristic artifact resolved with counter-evidence, mirroring Phases 2/3 — no blocking checkpoint warranted; no `[SLOP]` packages |
</threat_model>

<verification>
Phase-gate checks for this plan's wave (per 04-VALIDATION.md:30-32 sampling rate):

- Backend regression: `cd backend && uv run --extra dev pytest -q` (full suite, incl. the new env-read test)
- Lint: `cd backend && uv run --extra dev ruff check app/main.py tests/test_app.py`
- Frontend regression (the Dockerfile bakes the frontend build, so the source must stay healthy): `cd frontend && npx vitest run` and `cd frontend && npm run build`
- Docker: `docker build -t finally:latest .` exit 0
- Tracer end-to-end chain (Task 3 verify) green: health poll, static `/`, SSE frame, trade survives `docker restart`, non-root identity, `/app/db/finally.db` present
</verification>

<success_criteria>
- A single `docker run -v finally-data:/app/db -p 8000:8000 finally:latest` serves the terminal at http://localhost:8000 with streaming prices and a working API (success criterion 1 of the phase)
- SQLite persists across a container restart: a REST buy survives `docker restart` with cash below $10,000 (success criterion 2)
- Backend pytest + ruff, frontend vitest + build, and the Docker build are all green on the committed state
- The wave-1 merge leaves a running `finally` container on :8000 for 04-02's idempotency exercises
</success_criteria>

<output>
Create `.planning/phases/04-deployment-e2e/04-01-SUMMARY.md` when done
</output>
