---
phase: 04-deployment-e2e
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - test/tests/fresh-start.spec.ts
  - test/tests/watchlist.spec.ts
  - test/tests/trading.spec.ts
  - test/tests/visualizations.spec.ts
  - test/tests/chat.spec.ts
  - test/tests/sse-reconnect.spec.ts
autonomous: true
requirements: [TEST-02]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "the six specs pass serially against the compose app: fresh-start ($10,000.00 cash, ten sparklines, connection dot connected, price changes), watchlist (add PYPL → streams; remove → gone), trading (buy 10 AAPL → cash down + position; sell 5 → cash up, qty 5), visualizations (heatmap cells, P&L chart, positions table), chat ([mock] Acknowledged + AAPL buy confirmation), sse-reconnect (connected → offline → reconnecting → online → connected)"
    - "run-e2e (down -v → up --build -d → npx playwright test → down -v) exits 0 — the deterministic fresh-state gate (Pitfall 4)"
    - "specs assert only the selectors the frontend already ships: data-testid connection-dot/sparkline-{ticker}/heatmap-cell-{name}, aria-label 'connection: connected|reconnecting|closed', role buttons Buy/Sell/Add, placeholder 'Ask the AI to trade…', Remove {ticker} buttons (04-PATTERNS.md:367-393)"
  artifacts:
    - test/tests/fresh-start.spec.ts
    - test/tests/watchlist.spec.ts
    - test/tests/trading.spec.ts
    - test/tests/visualizations.spec.ts
    - test/tests/chat.spec.ts
    - test/tests/sse-reconnect.spec.ts
  key_links:
    - "spec selectors ↔ frontend/components/header/Header.tsx (connection-dot aria-label + DOT_CLASS), frontend/hooks/usePriceStream.ts (onerror → reconnecting, never close — backs sse-reconnect), frontend/tests/TerminalApp.test.tsx (proven selector queries)"
    - "chat.spec mock assertions ↔ backend/app/chat/service.py:55-59 deterministic mock shape ([mock] Acknowledged + trades [{ticker AAPL, side buy, quantity 1}])"
    - "fresh-state determinism ↔ run-e2e's leading down -v wiping finally-test-data (04-03)"
---

<objective>
The six TEST-02 E2E scenarios authored against the real selectors the Phase 3 frontend ships, run inside the playwright container against the compose app with LLM_MOCK=true: fresh start, watchlist add/remove, buy/sell, visualizations, mocked AI chat, and SSE reconnection. Task 2's full-suite run is the phase gate for TEST-02.

Purpose: these specs are the user-visible proof of the phase success criteria — the same assertions a human would click through in a browser, automated and reproducible from a deterministic $10k fresh state every run.
Output: six committed spec files and a green full-suite run via test/run-e2e.ps1 (host is Windows) or run-e2e.sh.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-e2e/04-RESEARCH.md
@.planning/phases/04-deployment-e2e/04-PATTERNS.md
@.planning/phases/04-deployment-e2e/04-VALIDATION.md
@frontend/components/header/Header.tsx
@frontend/hooks/usePriceStream.ts
@frontend/tests/TerminalApp.test.tsx
@backend/app/chat/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: fresh-start, watchlist, trading specs + targeted run</name>
  <files>test/tests/fresh-start.spec.ts, test/tests/watchlist.spec.ts, test/tests/trading.spec.ts</files>
  <read_first>
    - 04-PATTERNS.md:358-421 (spec selector source of truth: connection-dot aria-label lines 367-376, proven queries 378-393, spec-to-analog table 397-404)
    - 04-RESEARCH.md:28 (TEST-02 selector table), 445-453 (requirements-to-test map)
    - frontend/tests/TerminalApp.test.tsx:140-268 (the exact assertions these specs mirror — Test 1/2 fresh start, Test 5 trading, Test 6 watchlist)
  </files>test/tests/fresh-start.spec.ts, test/tests/watchlist.spec.ts, test/tests/trading.spec.ts</files>
  <action>
    Author three spec files, each `import { test, expect } from '@playwright/test';` and each starting with `await page.goto('/')`. Assert ONLY selectors the frontend ships (04-PATTERNS.md:367-393): connection-dot by aria-label (`connection: connected|reconnecting|closed`), sparklines by `page.getByTestId('sparkline-{TICKER}')`, heatmap cells by `getByTestId('heatmap-cell-{NAME}')`, trade bar via `getByLabel('Ticker')` / `getByLabel('Quantity')` / `getByRole('button', { name: 'Buy' })` / Sell, chat via `getByPlaceholder('Ask the AI to trade…')`, watchlist remove via `getByRole('button', { name: 'Remove {TICKER}' })`.

    test/tests/fresh-start.spec.ts (mirrors TerminalApp Test 1-2): assert the header cash shows $10,000.00; all ten seed tickers (AAPL GOOGL MSFT AMZN TSLA NVDA META JPM V NFLX) render sparkline-{TICKER} testids; connection-dot has aria-label 'connection: connected'; and streaming is live — capture one price cell text, wait ~3s, assert the text changed (prices stream at 20Hz from the simulator).

    test/tests/watchlist.spec.ts (mirrors TerminalApp Test 6): add PYPL via the add-ticker input + Add button; assert `sparkline-PYPL` appears AND its price text is non-empty and changes within ~3s (new ticker streams); then click the row's Remove PYPL button (204 empty-body DELETE — the frontend prunes locally, 03-07/03-06 wiring); assert sparkline-PYPL is gone.

    test/tests/trading.spec.ts (mirrors TerminalApp Test 5, POST body {ticker, quantity, side}): buy 10 AAPL via Ticker/AAPL + Quantity/10 + Buy; assert the header/portfolio cash is below $10,000.00 and an AAPL position row appears; then sell 5 AAPL (Sell button); assert cash increased from the post-buy value and the AAPL position quantity shows 5. NOTE: this spec intentionally runs AFTER fresh-start in serial order and must tolerate its own earlier mutations — determinism comes from run-e2e's down -v, not within-suite isolation (A7).

    Then run the targeted subset in the compose stack (PowerShell host; the app image and playwright image exist from 04-03):
    `docker compose -f test/docker-compose.test.yml up -d --build --wait`
    `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test tests/fresh-start.spec.ts tests/watchlist.spec.ts tests/trading.spec.ts`
    `docker compose -f test/docker-compose.test.yml down -v`
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up -d --build --wait; docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test tests/fresh-start.spec.ts tests/watchlist.spec.ts tests/trading.spec.ts; $s=$LASTEXITCODE; docker compose -f test/docker-compose.test.yml down -v; exit $s</automated>
  </verify>
  <acceptance_criteria>
    - The targeted run of fresh-start, watchlist, and trading specs passes with 0 failures in the playwright container (fresh DB via the leading up/--wait on a wiped volume or an explicit down -v before the up)
    - fresh-start.spec.ts asserts $10,000.00, ten sparkline testids, aria-label 'connection: connected', and a live price change
    - watchlist.spec.ts proves PYPL add → streams → remove round-trip against sparkline-PYPL
    - trading.spec.ts proves buy 10 AAPL (cash below $10,000, position appears) then sell 5 (cash up, quantity 5)
  </acceptance_criteria>
  <done>Three core-flow specs written and green in the compose stack: fresh start, watchlist CRUD, buy/sell.</done>
</task>

<task type="auto">
  <name>Task 2: visualizations, chat, sse-reconnect specs + full-suite phase gate</name>
  <files>test/tests/visualizations.spec.ts, test/tests/chat.spec.ts, test/tests/sse-reconnect.spec.ts</files>
  <read_first>
    - 04-RESEARCH.md:333-349 (sse-reconnect skeleton — copy verbatim), 192 (Pattern 5 setOffline + CDP fallback), 242-247 (Pitfall 5), 27 (LLM_MOCK call-time read + mock shape)
    - 04-PATTERNS.md:395-421 (spec-to-analog table + the sse-reconnect spec verbatim at 406-419)
    - backend/app/chat/service.py:38-59 (mock response shape: message "[mock] Acknowledged: ...", trades [{ticker AAPL, side buy, quantity 1}])
  </files>test/tests/visualizations.spec.ts, test/tests/chat.spec.ts, test/tests/sse-reconnect.spec.ts</files>
  <action>
    Author the remaining three specs in the same style as Task 1:

    test/tests/visualizations.spec.ts (mirrors TerminalApp Test 4): assert heatmap cells render — at least `heatmap-cell-AAPL` and one more seed ticker (getByTestId); assert the P&L chart container and the positions table are visible (portfolio slot renders live data); assert a positions-table value updates after the portfolio poll or a trade — keep to existence + one live-data assertion to stay deterministic.

    test/tests/chat.spec.ts: with LLM_MOCK=true the backend returns the deterministic shape from backend/app/chat/service.py:55-59 — message "[mock] Acknowledged: ..." with trades [{ticker AAPL, side buy, quantity 1}]. Send any message via the chat input (placeholder 'Ask the AI to trade…') + Send button; assert the mock message text appears, and the inline AAPL buy confirmation renders (the frontend renders confirmations only from the structured trades field — 03-07/03-05 wiring); assert cash decreased by the AAPL buy if the confirmation shows quantity 1.

    test/tests/sse-reconnect.spec.ts: copy the RESEARCH skeleton verbatim (04-RESEARCH.md:337-348 / 04-PATTERNS.md:407-419): goto '/', assert connection-dot aria-label 'connection: connected'; capture the first price cell text; `await context.setOffline(true)` (browser-level network loss — the primary trigger, Pattern 5); expect the dot to leave connected (`/reconnecting|closed/`, timeout 10s) — the frontend's EventSource onerror sets reconnecting and never calls es.close() (usePriceStream.ts:16-26), honoring the backend's retry: 1000; `await context.setOffline(false)`; expect 'connection: connected' again (timeout 15s) and a price cell different from the captured text. A3 [ASSUMED]: if setOffline does not tear down an established SSE socket in Chromium (dot stays green), switch the trigger to CDP `Network.emulateNetworkConditions` via `context.newCDPSession(page)` with {offline: true/false} — the assertion targets stay identical (04-RESEARCH.md:192, 245, 409). Verify which trigger works on the FIRST run and record it in the SUMMARY.

    Then run the FULL suite — the TEST-02 phase gate. From the repo root on this Windows host: `& ./test/run-e2e.ps1` (down -v → up --build -d → all six specs serial → unconditional down -v; exit code 0 = green). If run-e2e.ps1 cannot run (compose quirk), fall back to the sh equivalent via wsl or the manual sequence with the same final down -v.
  </action>
  <verify>
    <automated>& ./test/run-e2e.ps1; if($LASTEXITCODE -ne 0){throw 'E2E suite failed'}</automated>
  </verify>
  <acceptance_criteria>
    - The full six-spec suite passes via run-e2e (exit 0): fresh-start, watchlist, trading, visualizations, chat, sse-reconnect
    - visualizations.spec.ts asserts heatmap-cell-AAPL + at least one other cell, the P&L chart, and the positions table
    - chat.spec.ts asserts the "[mock] Acknowledged" message and an inline AAPL buy confirmation (mock shape per service.py:55-59)
    - sse-reconnect.spec.ts asserts the aria-label transition connected → (reconnecting|closed) → connected with a changed price cell; the SUMMARY records which trigger worked (setOffline or the CDP fallback)
    - run-e2e leaves no container or test volume behind (trailing down -v verified by `docker ps -a --filter "name=finally"` showing none)
  </acceptance_criteria>
  <done>Six E2E specs committed and the full suite green from a deterministic fresh state; SSE-reconnect trigger resolved and documented.</done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 04-04)

- `test/tests/fresh-start.spec.ts` — test(s): $10k cash, ten sparkline testids, connection dot connected, live price change
- `test/tests/watchlist.spec.ts` — test(s): add PYPL streams → remove PYPL disappears
- `test/tests/trading.spec.ts` — test(s): buy 10 AAPL (cash down, position) → sell 5 (cash up, qty 5)
- `test/tests/visualizations.spec.ts` — test(s): heatmap cells, P&L chart, positions table
- `test/tests/chat.spec.ts` — test(s): mocked chat message + inline AAPL buy confirmation
- `test/tests/sse-reconnect.spec.ts` — test(s): connected → offline → reconnecting|closed → online → connected + price change
- Resolution note for A3: which network-loss trigger (setOffline vs CDP emulateNetworkConditions) works in the pinned Chromium — recorded in the SUMMARY

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E browser → app container | specs drive the real UI over the compose network with LLM_MOCK=true; never touches production finally-data |
| spec assertions → frontend DOM | tests assert selectors only; no XSS-bearing markup is injected or asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Spoofing / Tampering | E2E container vs production data | high | mitigate | Specs run against the compose app with finally-test-data only (04-03); run-e2e's down -v resets state; trading/chat mutations never reach finally-data |
| T-04-01 | Information Disclosure | Dockerfile ENV / image layers | high | mitigate | LLM_MOCK=true is the only injected env in tests — non-secret; no API keys required for the suite (keyless deterministic chat) |
| T-04-02 | Elevation of Privilege | Container runtime | medium | mitigate | The playwright container is trusted test tooling isolated on the compose network (04-RESEARCH.md:53 — root acceptable for trusted E2E); the app container remains non-root from 04-01 |
| T-04-03 | Tampering | Base images / dependency supply chain | high | mitigate | Browser image tag v1.62.0-jammy matched to @playwright/test@1.62.0 (A6); npm ci from committed lockfile (04-03) |
| T-04-SC | Tampering | npm/pip installs | low | mitigate | No new installs in this plan; the audited @playwright/test pin (04-RESEARCH.md:82-93) is the only test tooling — first-party Microsoft, postinstall null |
</threat_model>

<verification>
Phase-gate (per 04-VALIDATION.md:30-32): the full E2E suite via run-e2e is green, plus the unit-suite regressions — `cd backend && uv run --extra dev pytest -q`, `cd frontend && npx vitest run`, and `cd frontend && npm run build` — all green on the committed state. A3's chosen SSE trigger is verified on the first run and recorded in the SUMMARY.
</verification>

<success_criteria>
- E2E tests pass for fresh start, watchlist add/remove, buy/sell, visualizations, mocked AI chat, and SSE reconnection (success criterion 4 of the phase)
- The suite is reproducible: a second consecutive full run also passes from the down -v reset (no shared-state bleed)
- run-e2e leaves the host clean: no containers, no test volume residue
</success_criteria>

<output>
Create `.planning/phases/04-deployment-e2e/04-04-SUMMARY.md` when done
</output>
