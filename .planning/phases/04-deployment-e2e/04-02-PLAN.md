---
phase: 04-deployment-e2e
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/start_mac.sh
  - scripts/stop_mac.sh
  - scripts/start_windows.ps1
  - scripts/stop_windows.ps1
autonomous: true
requirements: [DEPLOY-04]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "scripts/start_windows.ps1 run twice in a row exits 0 both times (idempotent: `docker rm -f finally` with suppressed error is a no-op when absent) and leaves one `finally` container serving :8000"
    - "scripts/stop_windows.ps1 removes the container but never the finally-data volume; a trade made before stop survives stop → start (DEPLOY-03 persistence via the volume)"
    - "start scripts build the image when missing, pass --env-file .env only when the file exists, and gate readiness on a /api/health poll (Pitfall 6) before printing the URL"
    - "the sh scripts are the verified logic mirrored for macOS/Linux (docker rm -f with 2>/dev/null || true, curl health poll)"
  artifacts:
    - scripts/start_mac.sh
    - scripts/stop_mac.sh
    - scripts/start_windows.ps1
    - scripts/stop_windows.ps1
  key_links:
    - "script container name `finally` + volume `finally-data:/app/db` ↔ the 04-01 tracer's running container (start's rm -f handles the live container)"
    - "health poll on /api/health ↔ backend/app/main.py health() returning {'status': 'healthy'} (04-PATTERNS.md:443-451)"
---

<objective>
Idempotent start/stop lifecycle for the single-container deployment (DEPLOY-04): four scripts — macOS/Linux shell + Windows PowerShell — that build if needed, run the `finally` container with the persistent volume and optional .env, poll /api/health before declaring readiness, and stop without ever removing the volume.

Purpose: the success criterion "start/stop scripts are idempotent — safe to run repeatedly" is what makes the one-command experience trustworthy for a human; the fixed container name + suppressed-error pattern is the entire trick (04-RESEARCH.md Pattern 3).
Output: scripts/start_mac.sh, scripts/stop_mac.sh, scripts/start_windows.ps1, scripts/stop_windows.ps1 — proven idempotent on this machine (Windows host; the ps1 pair gets the functional proof, the sh pair gets WSL syntax checks + logic mirror review).
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-e2e/04-RESEARCH.md
@.planning/phases/04-deployment-e2e/04-PATTERNS.md
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/start_mac.sh + scripts/stop_mac.sh (macOS/Linux, idempotent, health-poll gated)</name>
  <files>scripts/start_mac.sh, scripts/stop_mac.sh</files>
  <read_first>
    - 04-RESEARCH.md:351-365 (idempotent start/stop skeleton — copy verbatim), 180-183 (Pattern 3), 248-251 (Pitfall 6 health poll)
    - 04-PATTERNS.md:201-219 (sh script patterns: set -e, rm -f suppression, seq health loop, volume never removed)
  </files>scripts/start_mac.sh, scripts/stop_mac.sh</files>
  <action>
    Create scripts/start_mac.sh per the 04-RESEARCH.md:353-362 skeleton verbatim (Pattern 3, 04-PATTERNS.md:203-214):
    - `set -e` at the top.
    - Build if the image is missing: `docker image inspect finally:latest >/dev/null 2>&1 || docker build -t finally:latest .` (idempotent — a second run skips the build).
    - `docker rm -f finally 2>/dev/null || true` — the idempotency linchpin: suppressed-error rm is a no-op when the container is absent, so the script never errors on a second run, and it cleanly replaces a live container from 04-01.
    - `[ -f .env ] && ENV_ARGS="--env-file .env" || ENV_ARGS=""` — keys are passed at runtime only, never baked (T-04-01); with no root .env today the script runs without it.
    - `docker run -d --name finally -v finally-data:/app/db -p 8000:8000 $ENV_ARGS finally:latest` — no `--rm` (needs the named container for the stop model).
    - Health-poll gate (Pitfall 6): `for i in $(seq 1 30); do curl -sf http://localhost:8000/api/health >/dev/null 2>&1 && break; sleep 2; done` then a final `curl -sf` that prints `FinAlly running at http://localhost:8000` only when healthy.
    Create scripts/stop_mac.sh per 04-RESEARCH.md:363-364: `docker rm -f finally 2>/dev/null || true` with a comment that the finally-data volume is deliberately NOT removed — data survives (DEPLOY-03).
    Do not use `--workers` anywhere in the scripts (single-process invariant, 04-RESEARCH.md:196).
  </action>
  <verify>
    <automated>wsl bash -n scripts/start_mac.sh && wsl bash -n scripts/stop_mac.sh</automated>
  </verify>
  <acceptance_criteria>
    - `wsl bash -n scripts/start_mac.sh` and `wsl bash -n scripts/stop_mac.sh` both exit 0 (syntax-valid; WSL2 Ubuntu present per 04-RESEARCH.md:413 — if wsl is unavailable on the host, fall back to a line-by-line logic review against the verified ps1 pair and note it in the SUMMARY)
    - start_mac.sh contains: set -e, the image-inspect build guard, `docker rm -f finally 2>/dev/null || true`, the conditional `--env-file .env`, the fixed run line with `-v finally-data:/app/db -p 8000:8000`, and the 30-iteration health-poll loop
    - stop_mac.sh contains only the suppressed-error `docker rm -f finally` and no volume removal
  </acceptance_criteria>
  <done>macOS/Linux start/stop scripts created with the idempotency pattern (suppressed rm, build-if-missing, health-poll gate, volume-preserving stop).</done>
</task>

<task type="auto">
  <name>Task 2: scripts/start_windows.ps1 + scripts/stop_windows.ps1 + functional idempotency/persistence proof</name>
  <files>scripts/start_windows.ps1, scripts/stop_windows.ps1</files>
  <read_first>
    - 04-RESEARCH.md:183, 366 (PowerShell mirrors: 2>$null, Invoke-WebRequest -UseBasicParsing)
    - 04-PATTERNS.md:223-242 (PowerShell skeletons), 435-450 (shared patterns 2-3: idempotent lifecycle + health poll)
  </files>scripts/start_windows.ps1, scripts/stop_windows.ps1</files>
  <action>
    Create scripts/start_windows.ps1 mirroring the sh logic in PowerShell (04-PATTERNS.md:226-237):
    - `$ErrorActionPreference = 'Stop'` at the top.
    - Build guard: `docker image inspect finally:latest *> $null; if ($LASTEXITCODE -ne 0) { docker build -t finally:latest . }`.
    - `docker rm -f finally 2>$null | Out-Null` (2>$null mirrors 2>/dev/null — the idempotency linchpin).
    - `$envArgs = @(); if (Test-Path .env) { $envArgs = @('--env-file', '.env') }` — runtime-only key injection (T-04-01).
    - `docker run -d --name finally -v finally-data:/app/db -p 8000:8000 @envArgs finally:latest`, then the health-poll loop: `for ($i = 0; $i -lt 30; $i++) { try { Invoke-WebRequest -UseBasicParsing http://localhost:8000/api/health -TimeoutSec 3 | Out-Null; break } catch { Start-Sleep -Seconds 2 } }` followed by `Write-Host "FinAlly running at http://localhost:8000"`.
    Create scripts/stop_windows.ps1: `docker rm -f finally 2>$null | Out-Null` with the comment that finally-data is NOT removed (data survives).
    Then run the functional proof on this Windows host (the finally:latest image and finally-data volume exist from 04-01; the tracer's live container is expected and handled by the script's rm -f):
    1. `& ./scripts/start_windows.ps1` — exit 0, health-gated.
    2. `& ./scripts/start_windows.ps1` again — must ALSO exit 0 (idempotency: second start replaces the running container without error).
    3. `Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/portfolio/trade -ContentType 'application/json' -Body '{"ticker":"AAPL","side":"buy","quantity":3}' | Out-Null` — a trade to prove persistence.
    4. `& ./scripts/stop_windows.ps1` then `docker ps -a --filter "name=^finally$" --format '{{.Names}}'` — empty output (container gone, volume kept).
    5. `& ./scripts/start_windows.ps1` again, then `(Invoke-RestMethod -Uri http://localhost:8000/api/portfolio).cash_balance` — strictly below 10000, proving the trade survived stop → start through the volume (DEPLOY-03).
    6. `& ./scripts/stop_windows.ps1` to leave the host clean for wave 3.
  </action>
  <verify>
    <automated>& ./scripts/start_windows.ps1; if($LASTEXITCODE -ne 0){throw 'first start failed'}; & ./scripts/start_windows.ps1; if($LASTEXITCODE -ne 0){throw 'second start not idempotent'}; Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/portfolio/trade -ContentType 'application/json' -Body '{"ticker":"AAPL","side":"buy","quantity":3}' | Out-Null; & ./scripts/stop_windows.ps1; if(@(docker ps -a --filter 'name=^finally$' --format '{{.Names}}').Count -ne 0){throw 'container not removed'}; & ./scripts/start_windows.ps1; if((Invoke-RestMethod -Uri http://localhost:8000/api/portfolio).cash_balance -ge 10000){throw 'trade did not survive stop/start'}; & ./scripts/stop_windows.ps1</automated>
  </verify>
  <acceptance_criteria>
    - start_windows.ps1 run twice consecutively: both invocations exit 0
    - After stop, `docker ps -a --filter "name=^finally$"` lists no container, but the finally-data volume still exists (`docker volume ls` shows it)
    - A buy of 3 AAPL made before stop → start is visible after the restart: /api/portfolio cash_balance < 10000
    - stop_windows.ps1 contains no volume-removal command
  </acceptance_criteria>
  <done>Windows PowerShell scripts created and functionally proven on the host: double-start idempotent, stop preserves the volume, and a trade survives the stop → start cycle.</done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 04-02)

- `scripts/start_mac.sh` — build-if-missing → rm -f finally → run -d with finally-data:/app/db + optional --env-file .env → 30x2s /api/health poll → URL echo
- `scripts/stop_mac.sh` — rm -f finally only; volume never removed
- `scripts/start_windows.ps1` — PowerShell mirror (2>$null, Invoke-WebRequest -UseBasicParsing)
- `scripts/stop_windows.ps1` — PowerShell mirror
- Container contract: fixed name `finally`, volume `finally-data`, port 8000, env via --env-file only when present

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell → docker daemon | the scripts are host-side UX over `docker run`; they decide env injection and volume wiring |
| docker run env → container | secrets cross here at runtime only, never at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Dockerfile ENV / image layers | high | mitigate | Scripts pass `--env-file .env` only when the file exists — keys reach the container at `docker run` time, never via ENV in the Dockerfile or baked layers (04-01) |
| T-04-02 | Elevation of Privilege | Container runtime | medium | mitigate | Runtime runs non-root `USER app` (04-01); scripts add no privileged flags (no --privileged, no host mounts beyond the named volume) |
| T-04-06 | Spoofing / Tampering | E2E container vs production data | high | mitigate | Production scripts mount only `finally-data`; the E2E stack (04-03) uses the separate `finally-test-data` volume — the scripts never touch test data |
| T-04-SC | Tampering | npm/pip installs | low | mitigate | No new package installs in this plan; build steps reuse the audited lockfile-driven installs (04-RESEARCH.md:82-93) |
</threat_model>

<verification>
- Functional idempotency proof (Task 2 verify): double-start exit 0, stop leaves no container, trade survives stop → start, volume retained
- WSL syntax checks for the sh pair (Task 1 verify); if wsl is unavailable, the SUMMARY records the line-by-line mirror review against the ps1 pair
- The manual-only items from 04-VALIDATION.md:64-70 (repeated interactive start/stop UX, docker-run restart-persistence) are surfaced for human sign-off at the phase gate; the automated checks above cover their substance
</verification>

<success_criteria>
- Running the start script twice in a row succeeds both times (success criterion 3 of the phase: scripts idempotent)
- stop → start preserves positions, watchlist, and cash through the finally-data volume (success criterion 2)
- Both script pairs exist for macOS/Linux and Windows with identical lifecycle semantics
</success_criteria>

<output>
Create `.planning/phases/04-deployment-e2e/04-02-SUMMARY.md` when done
</output>
