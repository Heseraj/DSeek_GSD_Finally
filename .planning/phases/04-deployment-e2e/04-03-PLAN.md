---
phase: 04-deployment-e2e
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - test/package.json
  - test/package-lock.json
  - test/playwright.config.ts
  - test/playwright.Dockerfile
  - test/docker-compose.test.yml
  - test/run-e2e.sh
  - test/run-e2e.ps1
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "test/package.json pins @playwright/test exactly 1.62.0 (no caret) with a committed package-lock.json, and `npx playwright --version` reports 1.62.0"
    - "test/playwright.config.ts sets testDir ./tests, fullyParallel: false, workers: 1, retries: 0, chromium headless, baseURL from PLAYWRIGHT_BASE_URL with http://localhost:8000 fallback (A7 serial-only against one mutable DB)"
    - "test/docker-compose.test.yml boots app (build ../, LLM_MOCK=true, finally-test-data volume, urllib healthcheck) + playwright (FROM mcr.microsoft.com/playwright:v1.62.0-jammy, ipc: host, PLAYWRIGHT_BASE_URL=http://app:8000, depends_on app healthy, specs volume-mounted)"
    - "run-e2e.sh/.ps1 lead with `down -v` (wipes finally-test-data for the deterministic $10k fresh start — Pitfall 4) and always run `down -v` on exit regardless of test status"
  artifacts:
    - test/package.json
    - test/package-lock.json
    - test/playwright.config.ts
    - test/playwright.Dockerfile
    - test/docker-compose.test.yml
    - test/run-e2e.sh
    - test/run-e2e.ps1
  key_links:
    - "@playwright/test@1.62.0 exact pin ↔ mcr.microsoft.com/playwright:v1.62.0-jammy image tag — a patch mismatch yields 'browser executable not found' (04-RESEARCH.md:200, A6)"
    - "compose app service build: .. ↔ the root Dockerfile + .dockerignore from 04-01 — this plan must not run before 04-01"
    - "healthcheck python -c urllib ↔ slim runtime image ships no curl (04-RESEARCH.md:251); depends_on condition: service_healthy replaces the scripts' poll loop"
---

<objective>
The TEST-01 infrastructure: a `test/` directory holding the Playwright test runner (npm-scoped, exact-pinned 1.62.0), its config, the browser container image, the two-service test compose stack (app + playwright on their own network and their own volume), and the run-e2e orchestration scripts that make every suite start from a deterministic fresh $10k profile.

Purpose: E2E must never touch production data and must be reproducible. The dedicated `finally-test-data` volume plus the leading `down -v` is the entire determinism mechanism (Pattern 4 / Pitfall 4); the version-pinned official MCR image removes all browser-install friction (A6).
Output: a bootable `test/` stack — `docker compose -f test/docker-compose.test.yml build` produces both images; the six specs land on top in 04-04.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-e2e/04-RESEARCH.md
@.planning/phases/04-deployment-e2e/04-PATTERNS.md
@.planning/phases/04-deployment-e2e/04-VALIDATION.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: test/ npm package (@playwright/test@1.62.0 exact) + playwright.config.ts (serial, headless, env baseURL)</name>
  <files>test/package.json, test/package-lock.json, test/playwright.config.ts</files>
  <read_first>
    - 04-RESEARCH.md:72-80 (installation commands), 147-151 (project structure), 434-441 (Validation Architecture config contract), 199-201 (version-drift anti-pattern)
    - 04-PATTERNS.md:246-262 (package.json pattern: finally-e2e, exact pin, postinstall null), 264-284 (playwright.config.ts: workers:1, fullyParallel:false mandatory)
  </files>test/package.json, test/package-lock.json, test/playwright.config.ts</files>
  <action>
    Create the test/ npm package:
    1. Run `cd test && npm init -y` then `npm i -D @playwright/test@1.62.0` (RESEARCH.md:75) — the exact version arg is what produces the exact pin. Then edit test/package.json so the devDependencies entry reads exactly `"@playwright/test": "1.62.0"` (no caret — 04-PATTERNS.md:260), name `finally-e2e`, version 0.1.0, private true, and a `"test": "playwright test"` script. The generated test/package-lock.json is committed (reproducible `npm ci` in the image). No allowScripts entry needed: @playwright/test ships postinstall: null (audit, 04-RESEARCH.md:84).
    2. Create test/playwright.config.ts per 04-RESEARCH.md:439 (locked properties) and 04-PATTERNS.md:268-282: import { defineConfig } from '@playwright/test'; export default defineConfig({ testDir: './tests', fullyParallel: false, workers: 1, retries: 0, use: { baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:8000', headless: true }, projects: [{ name: 'chromium', use: { browserName: 'chromium' } }] }). workers: 1 + fullyParallel: false are MANDATORY (A7) — the six specs share one mutable SQLite DB; determinism comes from down -v in run-e2e, not test isolation.
    3. Confirm `npx playwright --version` in test/ prints 1.62.0 (matches the MCR image tag consumed in Task 2).
  </action>
  <verify>
    <automated>cd test && npm ls @playwright/test && npx playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @playwright/test` in test/ reports @playwright/test@1.62.0 and `npx playwright --version` prints 1.62.0
    - test/package.json devDependencies pin is exactly "1.62.0" (no ^ or ~); test/package-lock.json exists and is committed
    - test/playwright.config.ts contains testDir './tests', fullyParallel: false, workers: 1, retries: 0, headless: true, and the PLAYWRIGHT_BASE_URL ?? 'http://localhost:8000' fallback
  </acceptance_criteria>
  <done>test/ npm package and Playwright config in place with the exact version-to-image pin (1.62.0 both sides).</done>
</task>

<task type="auto">
  <name>Task 2: playwright.Dockerfile + docker-compose.test.yml + run-e2e.sh/.ps1 (down -v determinism)</name>
  <files>test/playwright.Dockerfile, test/docker-compose.test.yml, test/run-e2e.sh, test/run-e2e.ps1</files>
  <read_first>
    - 04-RESEARCH.md:299-331 (docker-compose.test.yml skeleton), 145-155 (playwright.Dockerfile + compose description), 127-131 (run-e2e orchestration), 236-240 (Pitfall 4), 187 (Pattern 4)
    - 04-PATTERNS.md:287-355 (playwright.Dockerfile, compose, run-e2e patterns)
  </files>test/playwright.Dockerfile, test/docker-compose.test.yml, test/run-e2e.sh, test/run-e2e.ps1</files>
  <action>
    Create test/playwright.Dockerfile: FROM mcr.microsoft.com/playwright:v1.62.0-jammy (official image — Chromium + system deps preinstalled; tag MUST match the npm 1.62.0 pin, A6), WORKDIR /test, COPY package.json package-lock.json ./ , RUN npm ci. Do NOT run `npx playwright install` (browsers already in the base image) and do NOT COPY tests — the compose volume ./tests:/test/tests delivers spec files live so 04-04's specs need no image rebuild.

    Create test/docker-compose.test.yml per 04-RESEARCH.md:299-331: service `app` with build: .. (repo root context — consumes the 04-01 Dockerfile + .dockerignore), environment LLM_MOCK=true (deterministic chat — mock reads env at call time, service.py:46), volumes finally-test-data:/app/db (a DIFFERENT volume from production finally-data — E2E never touches production data, Pattern 4), and healthcheck test ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"] with interval 2s / timeout 3s / retries 30 (slim image ships no curl, RESEARCH.md:251). Service `playwright` with build: { context: ., dockerfile: playwright.Dockerfile }, ipc: host (Chromium requirement), depends_on: app: { condition: service_healthy }, environment PLAYWRIGHT_BASE_URL=http://app:8000, volumes ./tests:/test/tests, working_dir /test, command: npx playwright test. Top-level volumes: finally-test-data:.

    Create test/run-e2e.sh per 04-RESEARCH.md:130 + 04-PATTERNS.md:343-353: set -e; `docker compose -f test/docker-compose.test.yml down -v` FIRST (the -v wipes finally-test-data → deterministic seeded $10k every run, Pitfall 4); `docker compose -f test/docker-compose.test.yml up --build -d`; then `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test` capturing the exit status; and a trailing `docker compose -f test/docker-compose.test.yml down -v` that ALWAYS runs (even on test failure) before `exit $status`.

    Create test/run-e2e.ps1 mirroring in PowerShell: $ErrorActionPreference = 'Stop'; `docker compose -f test/docker-compose.test.yml down -v --remove-orphans`; `docker compose -f test/docker-compose.test.yml up --build -d`; `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test`; `$status = $LASTEXITCODE`; trailing `docker compose -f test/docker-compose.test.yml down -v --remove-orphans`; `exit $status` (04-PATTERNS.md:354).
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml config --quiet</automated>
    <automated>docker compose -f test/docker-compose.test.yml build</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml config --quiet` exits 0 (compose file valid; the app build: .. resolves)
    - `docker compose -f test/docker-compose.test.yml build` exits 0, producing the `app` image (root Dockerfile, tagged under the compose project) and the `playwright` image (FROM mcr.microsoft.com/playwright:v1.62.0-jammy)
    - docker-compose.test.yml mounts finally-test-data (never finally-data), sets LLM_MOCK=true on app, and playwright has ipc: host + depends_on app service_healthy
    - run-e2e.sh and run-e2e.ps1 both contain a leading down -v and a trailing down -v that runs unconditionally
  </acceptance_criteria>
  <done>Test compose stack builds both images; run-e2e orchestration in place with deterministic fresh-state semantics.</done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 04-03)

- `test/package.json` — finally-e2e, private, script test → playwright test, devDependency @playwright/test exactly 1.62.0
- `test/package-lock.json` — committed lockfile for `npm ci` in the browser image
- `test/playwright.config.ts` — chromium project, headless, fullyParallel false, workers 1, retries 0, baseURL env fallback
- `test/playwright.Dockerfile` — FROM mcr.microsoft.com/playwright:v1.62.0-jammy + npm ci (browsers preinstalled; no install step)
- `test/docker-compose.test.yml` — services app (LLM_MOCK, finally-test-data, urllib healthcheck) + playwright (ipc host, PLAYWRIGHT_BASE_URL=http://app:8000, specs volume)
- `test/run-e2e.sh` + `test/run-e2e.ps1` — down -v → up --build -d → run playwright → unconditional down -v
- Volume contract: `finally-test-data` (test-only; production stays `finally-data`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E browser (playwright container) → app container | trusted test traffic on the compose network; must stay isolated from production data |
| host repo → build contexts | test/ and root contexts feed both images; .dockerignore guards secrets |
| LLM_MOCK env → chat service | mock mode must be forced for deterministic, keyless chat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Spoofing / Tampering | E2E container vs production data | high | mitigate | Dedicated `finally-test-data` volume in docker-compose.test.yml — never `finally-data`; run-e2e leads with `down -v`; E2E cannot reach the production volume (Pattern 4) |
| T-04-01 | Information Disclosure | Dockerfile ENV / image layers | high | mitigate | The app image is built from the root Dockerfile whose .dockerignore excludes .env/frontend/.env.local (04-01); LLM_MOCK=true is a test-only, non-secret env |
| T-04-03 | Tampering | Base images / dependency supply chain | high | mitigate | mcr.microsoft.com/playwright:v1.62.0-jammy tag pinned and matched to @playwright/test@1.62.0 (A6); npm ci from the committed test/package-lock.json |
| T-04-04 | Tampering | npm postinstall / script execution | medium | mitigate | npm ci from committed lockfile only; @playwright/test postinstall: null (audit 04-RESEARCH.md:84); no lifecycle-script surface |
| T-04-SC | Tampering | npm/pip installs | low | mitigate | Package Legitimacy Audit (04-RESEARCH.md:82-93) approved @playwright/test/playwright as first-party Microsoft, exact-pinned, postinstall null — mirrors Phases 2/3 resolution; no [SLOP]; no blocking checkpoint warranted |
</threat_model>

<verification>
- `docker compose -f test/docker-compose.test.yml config --quiet` validates the compose file
- `docker compose -f test/docker-compose.test.yml build` proves both images build (app from the root Dockerfile, playwright from the MCR base)
- Full E2E run is the 04-04 phase gate (specs land next wave); this plan's merge gate is the two commands above plus the config-content acceptance criteria
</verification>

<success_criteria>
- The test stack boots app + playwright with LLM_MOCK=true and a dedicated test volume (TEST-01)
- run-e2e scripts exist in both shell flavors with guaranteed fresh-state semantics
- Both compose images build cleanly from committed sources (no stale finally:latest or v1.48.0-jammy reuse)
</success_criteria>

<output>
Create `.planning/phases/04-deployment-e2e/04-03-SUMMARY.md` when done
</output>
