---
phase: 02-ai-chat-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/main.py
  - .env.example
  - backend/app/chat/schemas.py
  - backend/app/chat/prompts.py
  - backend/tests/chat/__init__.py
  - backend/tests/chat/test_service.py
autonomous: false
requirements: [CHAT-01]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "litellm>=1.98.0 and python-dotenv>=1.0 are installed and locked in backend/uv.lock"
    - "The backend loads the project-root .env at import time via load_dotenv(), so OPENROUTER_API_KEY and LLM_MOCK are readable through os.environ at call time"
    - "backend/app/chat/schemas.py defines ChatRequest, TradeAction, WatchlistChange, ChatProposal, and ChatResponse with per-action result models — the CHAT-01 response envelope"
    - "backend/app/chat/prompts.py defines SYSTEM_PROMPT beginning with 'FinAlly, an AI trading assistant' and the pure formatter build_context(portfolio, watchlist)"
  artifacts:
    - backend/pyproject.toml
    - backend/uv.lock
    - backend/app/main.py
    - .env.example
    - backend/app/chat/schemas.py
    - backend/app/chat/prompts.py
    - backend/tests/chat/__init__.py
    - backend/tests/chat/test_service.py
  key_links:
    - "main.py calls load_dotenv() before the FastAPI app is constructed so every later call-time env read sees .env values"
    - "schemas.py and prompts.py import cleanly with the newly added litellm dependency installed"
---

<objective>
Foundation for the chat subsystem: add the two runtime dependencies (litellm, python-dotenv), wire project-root `.env` loading into the backend entry point, and create the Pydantic schemas and prompt module that define the CHAT-01 response envelope.

Purpose: Everything later in the phase — the `POST /api/chat` tracer (02-02) and the live LiteLLM branch (02-03) — imports from `app.chat.schemas` and `app.chat.prompts`; this plan locks their shapes and proves the dependency stack before any endpoint code exists.
Output: `backend/app/chat/schemas.py`, `backend/app/chat/prompts.py`, `backend/tests/chat/` package start, updated `pyproject.toml` + `uv.lock`, dotenv-loaded `backend/app/main.py`, and a committed `.env.example`.
</objective>

<execution_context>
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/workflows/execute-plan.md
@C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-chat-assistant/02-RESEARCH.md
@.planning/phases/02-ai-chat-assistant/02-PATTERNS.md
@.planning/phases/02-ai-chat-assistant/COVERAGE.md
@backend/app/portfolio/schemas.py
@backend/app/watchlist/schemas.py
@backend/app/main.py
@backend/pyproject.toml
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Package legitimacy — human approval for litellm and python-dotenv</name>
  <what-built>Nothing installed yet — a blocking verification gate before `uv add litellm` / `uv add python-dotenv` (Task 2)</what-built>
  <how-to-verify>
    Both packages carry verdict [SUS] from the gate seam (a release-date heuristic artifact, counter-evidenced in RESEARCH: litellm first released 2023-07-27 with ~10M+ weekly downloads, first-party repo github.com/BerriAI/litellm, mandated by spec planning/PLAN.md §9; python-dotenv is 10+ yrs old at github.com/theskumar/python-dotenv). Verify each package on the PyPI registry and obtain explicit user approval:

    1. Fetch https://pypi.org/pypi/litellm/json — confirm the `name` field is "litellm", the `version` field is >= 1.98.0, and `info.home_page` / `info.project_urls` point at github.com/BerriAI/litellm.
    2. Fetch https://pypi.org/pypi/python-dotenv/json — confirm the `name` field is "python-dotenv" and the project links point at github.com/theskumar/python-dotenv.
    3. Present the findings to the user with the RESEARCH counter-evidence and ask for explicit approval of both packages.
    4. The task passes ONLY when the user approves both. If either is rejected, halt the plan and report back to the orchestrator — do not proceed to Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" to install both packages, or describe any concern about either package</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install litellm + python-dotenv, wire .env loading, add .env.example</name>
  <files>
    backend/pyproject.toml
    backend/uv.lock
    backend/app/main.py
    .env.example
  </files>
  <read_first>
    @backend/pyproject.toml
    @backend/app/main.py
    @.planning/phases/02-ai-chat-assistant/02-RESEARCH.md (Standard Stack + Pitfall 1)
  </read_first>
  <precondition>Task 1 human approval of both packages obtained.</precondition>
  <action>
    Work from the `backend/` directory (uv project root).

    1. Run `uv add litellm ">=1.98.0"` and `uv add python-dotenv ">=1.0"`. This appends to `[project].dependencies` in `backend/pyproject.toml` and regenerates `backend/uv.lock`. Keep the floor pins exactly as written — LiteLLM ships near-daily releases and the floor pin is the guard (RESEARCH Pitfall 1).
    2. In `backend/app/main.py`: add `from dotenv import load_dotenv` to the third-party import block (after the `from fastapi import FastAPI` line, before the local imports) and call `load_dotenv()` at module import time, after the imports and before the `app = FastAPI(...)` construction. Plain `load_dotenv()` walks up from the working directory, so a backend started from `backend/` reads the project-root `.env` — exactly what spec §5 promises. Do NOT read OPENROUTER_API_KEY or LLM_MOCK here: all env reads stay at call time inside chat/service.py (un-patchable import-time reads are an anti-pattern per RESEARCH).
    3. Create `.env.example` at the project root (C:/Local_Folder_Learning/AI_Engineer/AI_Coder/DSeek_GSD_Finally/.env.example) documenting, one per line with a short comment: OPENROUTER_API_KEY= (required for live chat; leave blank for mock), LLM_MOCK=false (set to true for deterministic mock responses, no key needed), MASSIVE_API_KEY= (optional; set for real market data). Placeholders only — no real secrets.
    4. Confirm `.gitignore` already ignores `.env` (it does — line `.env` under Environments). Do not add `.env.example` to .gitignore: it is meant to be committed.
  </action>
  <acceptance_criteria>
    `backend/pyproject.toml` `[project].dependencies` contains exactly "litellm>=1.98.0" and "python-dotenv>=1.0"; both also appear in `backend/uv.lock`; `backend/app/main.py` contains `load_dotenv()` invoked before `app = FastAPI(`; `.env.example` exists at project root with OPENROUTER_API_KEY, LLM_MOCK, and MASSIVE_API_KEY keys and no real secret values; `git check-ignore .env` exits 0 (env file ignored) while `.env.example` is NOT ignored.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import litellm, dotenv; assert tuple(map(int, litellm.__version__.split('.'))) >= (1, 98, 0); print(litellm.__version__)"</automated>
  </verify>
  <done>Both deps installed and locked, dotenv wired into main.py before app construction, .env.example committed, and the pre-existing suite still green.</done>
  <reversibility rating="reversible">`uv remove litellm python-dotenv` restores pyproject/lock and the main.py change is a two-line diff; nothing downstream depends on it yet.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (TDD): Chat schemas and prompts — RED then GREEN</name>
  <files>
    backend/app/chat/schemas.py
    backend/app/chat/prompts.py
    backend/tests/chat/__init__.py
    backend/tests/chat/test_service.py
  </files>
  <read_first>
    @backend/app/portfolio/schemas.py
    @backend/app/watchlist/schemas.py
    @.planning/phases/02-ai-chat-assistant/02-PATTERNS.md (schemas section)
  </read_first>
  <behavior>
    Write these tests first (RED — the modules do not exist yet, so pytest fails at collection; commit the failing state):

    - TestChatRequest: ChatRequest(message="  buy AAPL  ") keeps a non-empty message; ChatRequest(message="   ") raises ValidationError; ChatRequest(message="") raises ValidationError.
    - TestTradeAction: TradeAction(ticker=" aapl ", side="buy", quantity=1) normalizes ticker to "AAPL"; TradeAction(quantity=0) and TradeAction(quantity=-1) raise ValidationError (Field gt=0); TradeAction(side="hold") raises ValidationError (Literal buy|sell).
    - TestWatchlistChange: WatchlistChange(ticker="PYPL", action="add") parses; WatchlistChange(ticker="TOOLONGSYMBOL", action="add") raises ValidationError (max 12 chars); WatchlistChange(action="delete") raises ValidationError (Literal add|remove).
    - TestChatProposalParse: ChatProposal.model_validate_json('{"message":"hi","trades":[{"ticker":"AAPL","side":"buy","quantity":1}],"watchlist_changes":[]}') parses with trades[0].ticker == "AAPL"; a payload with no "trades" key parses with trades == []; a payload missing "message" raises ValidationError.
    - TestChatResponseShape: the no-key envelope JSON {"message":"LLM backend unavailable","trades":[],"watchlist_changes":[],"error":"OPENROUTER_API_KEY is not set"} validates as ChatResponse with error set and empty arrays; ChatResponse(message="ok") has default empty trades/watchlist_changes and error None; a response with trades=[{"ticker":"AAPL","side":"buy","quantity":1,"status":"executed"}] validates (status Literal executed|failed).
    - TestSystemPrompt: SYSTEM_PROMPT starts with "FinAlly, an AI trading assistant".
    - TestBuildContext: build_context({"cash_balance": 9620.0, "total_value": 10000.0, "positions": [{"ticker": "AAPL", "quantity": 2.0, "avg_cost": 190.0}]}, {"tickers": [{"ticker": "AAPL"}, {"ticker": "GOOGL"}]}) returns a string containing "AAPL", "9620.0", and "Watchlist"; build_context({}, {}) returns a non-empty string with no exception.
  </behavior>
  <action>
    Implement after the RED commit (GREEN):

    1. `backend/app/chat/schemas.py` — module docstring "Pydantic request/response schemas for the chat API." with `from __future__ import annotations`, `from typing import Annotated, Literal`, `from pydantic import BaseModel, Field, StringConstraints, field_validator`. Define:
       - `ChatRequest`: `message` as `Annotated[str, StringConstraints(strip_whitespace=True, min_length=1)]` so whitespace-only bodies 422 (mirrors WatchlistAddRequest, threat T-02-01).
       - `TradeAction` (LLM-proposal trade): `ticker: str` with a `field_validator` named `normalize_ticker` that returns `value.strip().upper()` (copy the TradeRequest pattern from portfolio/schemas.py); `quantity: float = Field(gt=0)`; `side: Literal["buy", "sell"]`.
       - `WatchlistChange`: `ticker` as `TickerStr`-style `Annotated[str, StringConstraints(strip_whitespace=True, min_length=1, max_length=12)]`; `action: Literal["add", "remove"]`.
       - `TradeActionResult`: `ticker: str`, `side: Literal["buy", "sell"]`, `quantity: float`, `status: Literal["executed", "failed"]`, `error: str | None = None`.
       - `WatchlistChangeResult`: `ticker: str`, `action: Literal["add", "remove"]`, `status: Literal["executed", "failed"]`, `error: str | None = None`.
       - `ChatProposal` (the LLM output envelope, parsed via `model_validate_json`): `message: str`, `trades: list[TradeAction] = []`, `watchlist_changes: list[WatchlistChange] = []`.
       - `ChatResponse` (the HTTP response envelope): `message: str`, `trades: list[TradeActionResult] = []`, `watchlist_changes: list[WatchlistChangeResult] = []`, `error: str | None = None`.
       Design note: two distinct models (proposal vs response) — the LLM emits status-less proposals; the service enriches them into status-carrying results for the wire. Do NOT reuse TradeRequest as the wire schema; the executor constructs `TradeRequest(**proposal)` internally.
    2. `backend/app/chat/prompts.py` — module docstring; `from __future__ import annotations`. Define `SYSTEM_PROMPT: str` starting "FinAlly, an AI trading assistant" and instructing the model to: analyze portfolio composition, risk concentration, and P&L; suggest trades with reasoning; execute trades when the user asks or agrees; manage the watchlist proactively; be concise and data-driven; and ALWAYS respond with valid JSON matching the envelope schema (message, trades, watchlist_changes). Define `build_context(portfolio: dict, watchlist: dict) -> str`: a pure formatter with no DB or cache access — first line "Portfolio: cash_balance=<cash>; total_value=<total>; unrealized_pnl=<pnl>", one line per position "POS <ticker> qty=<q> avg_cost=<a> price=<p> pnl=<pnl>", then "Watchlist: <comma-joined ticker symbols>". Use values straight from the dicts returned by get_portfolio/get_watchlist.
    3. `backend/tests/chat/__init__.py` — empty file (copy of tests/portfolio/__init__.py convention).
    4. `backend/tests/chat/test_service.py` — one test class per model plus TestSystemPrompt and TestBuildContext as listed in the behavior block. Follow the repo's double-quote/ruff conventions.
  </action>
  <acceptance_criteria>
    test_service.py contains the classes TestChatRequest, TestTradeAction, TestWatchlistChange, TestChatProposalParse, TestChatResponseShape, TestSystemPrompt, TestBuildContext; schemas.py exports ChatRequest, TradeAction, WatchlistChange, TradeActionResult, WatchlistChangeResult, ChatProposal, ChatResponse; prompts.py exports SYSTEM_PROMPT and build_context; the first commit (tests only) fails at pytest collection (modules absent), the second commit (implementation) passes the whole file.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/chat/test_service.py -q</automated>
  </verify>
  <done>All schema, prompt, and build_context tests pass; RED commit recorded before GREEN; ruff check and ruff format clean on the new files.</done>
  <reversibility rating="reversible">Schemas and prompt text are new pure-code modules; any shape change is a refactor covered by the tests, and nothing else imports them yet.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

New files:
- `backend/app/chat/schemas.py` — classes `ChatRequest`, `TradeAction`, `WatchlistChange`, `TradeActionResult`, `WatchlistChangeResult`, `ChatProposal`, `ChatResponse`
- `backend/app/chat/prompts.py` — constant `SYSTEM_PROMPT`, function `build_context(portfolio: dict, watchlist: dict) -> str`
- `backend/tests/chat/__init__.py` — empty test-package marker
- `backend/tests/chat/test_service.py` — test classes TestChatRequest, TestTradeAction, TestWatchlistChange, TestChatProposalParse, TestChatResponseShape, TestSystemPrompt, TestBuildContext
- `.env.example` — documents OPENROUTER_API_KEY / LLM_MOCK / MASSIVE_API_KEY

Modified files:
- `backend/pyproject.toml` — `[project].dependencies` gains `litellm>=1.98.0`, `python-dotenv>=1.0`
- `backend/uv.lock` — regenerated by `uv add`
- `backend/app/main.py` — adds `from dotenv import load_dotenv` + `load_dotenv()` before app construction

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → backend process | The project-root `.env` (gitignored) feeds secrets into the process via load_dotenv; `.env.example` (committed) contains placeholders only |
| dependency registry → codebase | Two new runtime packages are installed into the locked environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | ChatRequest / TradeAction / WatchlistChange Pydantic models | high | mitigate | LLM and user input both pass Pydantic validation: ChatRequest strips whitespace and rejects empty messages (422); TradeAction reuses the TradeRequest ticker-normalization and Field(gt=0) quantity bounds so a prompt-injected proposal cannot bypass manual-trade constraints; WatchlistChange bounds tickers to 1-12 chars via StringConstraints (mirrors WatchlistAddRequest) |
| T-02-02 | Information Disclosure | .env / OPENROUTER_API_KEY | high | mitigate | `.env` is gitignored (verified); `.env.example` ships placeholders only; the key is read at call time and never logged or echoed in responses |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate: blocking-human checkpoint (Task 1) verified litellm and python-dotenv against PyPI metadata and source repos before Task 2 runs `uv add`; no [SLOP] packages present; uv.lock committed for reproducibility |
</threat_model>

<verification>
- Per-task: `cd backend && uv run --extra dev pytest tests/chat/test_service.py -q` (Task 3) and the litellm import check (Task 2)
- Regression: `cd backend && uv run --extra dev pytest tests/test_app.py -q` must stay green after the main.py dotenv edit
- Lint: `cd backend && uv run --extra dev ruff check app/ tests/` clean on touched files
</verification>

<success_criteria>
- litellm and python-dotenv installed, human-approved, and locked
- Backend loads project-root .env at import time
- Chat schemas define the complete CHAT-01 envelope (proposal + response + result models)
- SYSTEM_PROMPT and build_context exist and are unit-tested
- Baseline suite (115 tests) still green
</success_criteria>

<output>
Create `.planning/phases/02-ai-chat-assistant/02-01-SUMMARY.md` when done
</output>
